# AI Анализ Результатов Опроса

## Описание

Endpoint для автоматического анализа результатов опроса с помощью GigaChat AI. Генерирует краткий профессиональный анализ с выводами и рекомендациями.

---

## Серверная часть (Backend)

### Endpoint

**GET** `/api/polls/:id/analyze`

### Параметры

- `id` (path parameter) - ID опроса для анализа

### Авторизация

Требуется аутентификация (JWT токен)

### Ответ

**Успешный ответ (200 OK):**

```json
{
  "success": true,
  "analysis": "1. ЛИДЕР: \"Вариант А\" победил с убедительным преимуществом в 45% голосов, что значительно превышает другие варианты.\n\n2. АКТИВНОСТЬ: Приняли участие 150 студентов, что демонстрирует высокую вовлеченность аудитории в опрос.\n\n3. ИНСАЙТ: Явное лидерство одного варианта указывает на единодушное мнение участников. Рекомендуется учесть этот результат при принятии решений."
}
```

**Ошибка - опрос не найден (404 Not Found):**

```json
{
  "success": false,
  "message": "Опрос не найден"
}
```

**Ошибка - проблема с AI (500 Internal Server Error):**

```json
{
  "success": false,
  "message": "Не удалось получить анализ от AI"
}
```

### Реализация

**Файл:** `server/src/controllers/pollController.js`

**Функция:** `analyzeResults`

#### Основные этапы:

1. **Получение данных опроса** - загружает опрос из БД с информацией о создателе
2. **Подготовка данных** - формирует структурированные данные о результатах с процентами
3. **Генерация промпта** - создаёт специализированный промпт для AI
4. **Вызов GigaChat API** - отправляет запрос на анализ
5. **Возврат результата** - отправляет клиенту готовый анализ

#### Структура промпта:

```text
Проанализируй результаты опроса и дай краткий профессиональный анализ.

Название опроса: [название]
Описание: [описание]
Тип: [single/multiple/rating]
Всего проголосовало: [количество]

Результаты:
1. "[вариант 1]" - [голосов] голосов ([процент]%)
2. "[вариант 2]" - [голосов] голосов ([процент]%)
...

Дай анализ в следующем формате (без лишнего текста, только суть):

1. ЛИДЕР: Кто победил и насколько убедительно (1-2 предложения)
2. АКТИВНОСТЬ: Оценка активности участников (1-2 предложения)
3. ИНСАЙТ: Главный вывод или рекомендация (1-2 предложения)

Пиши профессионально, по-русски, без лишних слов.
```

### Маршрут

**Файл:** `server/src/routes/pollRoutes.js`

```javascript
router.get('/:id/analyze', authenticate, pollController.analyzeResults);
```

**⚠️ Важно:** Роут `/:id/analyze` должен быть размещён **ПЕРЕД** общим роутом `/:id`, иначе Express будет воспринимать "analyze" как ID опроса.

---

## Клиентская часть (Frontend)

### Сервис

**Файл:** `client/src/services/pollService.js`

**Функция:** `analyzePollResults`

#### Использование:

```javascript
import { analyzePollResults } from '../services/pollService';

// Получить анализ результатов опроса
const handleAnalyze = async (pollId) => {
  try {
    const response = await analyzePollResults(pollId);
    console.log(response.analysis);
    // Отобразить анализ пользователю
  } catch (error) {
    console.error('Ошибка при анализе:', error);
  }
};
```

### Пример интеграции в компонент

```javascript
import React, { useState } from 'react';
import { analyzePollResults } from '../services/pollService';
import { Button, CircularProgress, Paper, Typography } from '@mui/material';

const PollAnalysis = ({ pollId }) => {
  const [analysis, setAnalysis] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const handleAnalyze = async () => {
    setLoading(true);
    setError('');
    
    try {
      const response = await analyzePollResults(pollId);
      setAnalysis(response.analysis);
    } catch (err) {
      setError('Не удалось получить анализ. Попробуйте позже.');
    } finally {
      setLoading(false);
    }
  };

  return (
    <div>
      <Button 
        variant="contained" 
        color="primary" 
        onClick={handleAnalyze}
        disabled={loading}
      >
        {loading ? <CircularProgress size={24} /> : 'Анализировать результаты с AI'}
      </Button>

      {error && (
        <Typography color="error" sx={{ mt: 2 }}>
          {error}
        </Typography>
      )}

      {analysis && (
        <Paper sx={{ p: 3, mt: 3 }}>
          <Typography variant="h6" gutterBottom>
            AI Анализ результатов
          </Typography>
          <Typography 
            component="pre" 
            sx={{ whiteSpace: 'pre-wrap', fontFamily: 'inherit' }}
          >
            {analysis}
          </Typography>
        </Paper>
      )}
    </div>
  );
};

export default PollAnalysis;
```

---

## Зависимости

### GigaChat Service

Endpoint использует `gigachatService.sendChatRequest()` для взаимодействия с GigaChat API.

**Требования:**
- Переменная окружения `GIGACHAT_AUTH_KEY` должна быть настроена
- GigaChat API должен быть доступен

### Модель Poll

Использует модель `Poll` с методами:
- `findById()` - поиск опроса
- `populate()` - загрузка связанных данных

---

## Примеры использования

### 1. Анализ активного опроса

```javascript
const pollId = '6502f5d8a8e1234567890abc';
const result = await analyzePollResults(pollId);
console.log(result.analysis);
```

### 2. Анализ с обработкой ошибок

```javascript
try {
  const result = await analyzePollResults(pollId);
  if (result.success) {
    displayAnalysis(result.analysis);
  }
} catch (error) {
  if (error.response?.status === 404) {
    showError('Опрос не найден');
  } else if (error.response?.status === 500) {
    showError('Ошибка AI сервиса');
  } else {
    showError('Произошла ошибка');
  }
}
```

---

## Особенности

### Формат анализа

AI возвращает структурированный анализ в формате:

1. **ЛИДЕР** - информация о победившем варианте
2. **АКТИВНОСТЬ** - оценка вовлечённости участников
3. **ИНСАЙТ** - ключевой вывод и рекомендации

### Temperature

Для анализа используется `temperature = 0.7`, что обеспечивает баланс между креативностью и точностью ответов.

### Обработка ошибок

- Проверка существования опроса
- Валидация ответа от AI
- Логирование ошибок для отладки
- Понятные сообщения об ошибках для пользователя

---

## Тестирование

### Проверка endpoint

```bash
# Получить токен
POST http://localhost:5000/api/auth/login
{
  "email": "user@example.com",
  "password": "password123"
}

# Анализировать опрос
GET http://localhost:5000/api/polls/[POLL_ID]/analyze
Authorization: Bearer [YOUR_TOKEN]
```

### Проверка через Postman/Insomnia

1. Создайте опрос и запишите его ID
2. Проголосуйте в опросе для получения результатов
3. Отправьте GET запрос на `/api/polls/:id/analyze`
4. Проверьте структуру ответа

---

## Производительность

- Среднее время ответа: 2-5 секунд (зависит от GigaChat API)
- Кэширование токена GigaChat для оптимизации
- Рекомендуется добавить индикатор загрузки на фронтенде

---

## Безопасность

- ✅ Требуется аутентификация через JWT
- ✅ Проверка существования опроса
- ✅ Валидация ID опроса (MongoDB ObjectId)
- ✅ Обработка ошибок без утечки информации

---

## Возможные улучшения

1. **Кэширование анализов** - сохранять результаты анализа в БД
2. **Расширенный анализ** - добавить графики и визуализацию
3. **Сравнительный анализ** - сравнение с похожими опросами
4. **Экспорт** - возможность скачать анализ в PDF/Word
5. **История анализов** - отслеживание изменений во времени

---

## Troubleshooting

### Проблема: "Не удалось получить анализ от AI"

**Решение:**
- Проверьте `GIGACHAT_AUTH_KEY` в `.env`
- Убедитесь, что GigaChat API доступен
- Проверьте логи сервера для деталей

### Проблема: "Опрос не найден"

**Решение:**
- Проверьте корректность ID опроса
- Убедитесь, что опрос существует в БД
- Проверьте формат ID (должен быть MongoDB ObjectId)

### Проблема: Медленный ответ

**Решение:**
- Это нормально для AI запросов (2-5 сек)
- Добавьте индикатор загрузки
- Рассмотрите возможность кэширования

---

## Changelog

### v1.0.0 - 2025-12-18
- ✨ Добавлен endpoint `/api/polls/:id/analyze`
- ✨ Добавлена функция `analyzeResults` в контроллер
- ✨ Добавлена клиентская функция `analyzePollResults`
- 📝 Создана документация























