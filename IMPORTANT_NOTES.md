# ⚠️ Важные замечания по реализации

## Что нужно сделать перед запуском

### 1. Обновить существующих пользователей в базе данных

Если у вас уже есть пользователи в базе, им нужно добавить новые поля:

```javascript
// Подключитесь к MongoDB shell или используйте MongoDB Compass

// Для студентов
db.users.updateMany(
  { 
    role: 'student',
    faculty_name: { $exists: false }
  },
  {
    $set: {
      faculty_name: "$faculty",
      program_name: "$program", 
      group_name: "$group"
    }
  }
)
```

**Альтернатива:** Создать миграционный скрипт:

```javascript
// server/src/scripts/migrateUserFields.js

const mongoose = require('mongoose');
const User = require('../models/User');
require('../config/database'); // Подключение к БД

const migrateUsers = async () => {
  try {
    console.log('🔄 Начинаем миграцию пользователей...');
    
    const users = await User.find({ 
      role: 'student',
      faculty_name: { $exists: false }
    });
    
    console.log(`Найдено ${users.length} пользователей для обновления`);
    
    for (const user of users) {
      // Копируем значения из faculty/program/group
      user.faculty_name = user.faculty_name || user.faculty;
      user.program_name = user.program_name || user.program;
      user.group_name = user.group_name || user.group;
      
      await user.save();
      console.log(`✅ Обновлен пользователь: ${user.email}`);
    }
    
    console.log('✅ Миграция завершена!');
    process.exit(0);
  } catch (error) {
    console.error('❌ Ошибка миграции:', error);
    process.exit(1);
  }
};

migrateUsers();
```

Запуск:
```bash
cd server
node src/scripts/migrateUserFields.js
```

---

### 2. Перезапустить сервер

После обновления моделей:

```bash
cd server
npm run dev
```

Проверьте, что сервер запускается без ошибок.

---

### 3. Проверить существующие опросы

**Важно:** Старые опросы без метаданных в responses работать будут, но аналитика для них будет пустой.

**Варианты:**
1. Оставить как есть (старые опросы = старая система)
2. Пометить старые опросы как `type: 'custom'`
3. Создать новые опросы с новой структурой

---

## Известные ограничения

### 1. Метаданные сохраняются только при новых голосах

Если у вас уже есть голоса в опросах, они **не содержат метаданных**.

**Решение:** 
- Новые голоса будут с метаданными
- Для старых опросов аналитика не будет работать
- Можно создать новые опросы для тестирования

---

### 2. Fallback для отсутствующих полей

Если у пользователя нет `faculty_name`, система использует `faculty`:

```javascript
faculty_name: userMetadata.faculty_name || userMetadata.faculty
```

Это работает, но лучше заполнить реальными названиями.

---

### 3. Проверка прав доступа к аналитике

Только **создатель опроса** или **админ** может видеть аналитику:

```javascript
if (poll.creator_id.toString() !== req.user.userId.toString() && req.user.role !== 'admin') {
  return res.status(403).json({ message: 'Нет доступа' });
}
```

Если нужно дать доступ всем - убрать эту проверку.

---

## Частые ошибки при тестировании

### Ошибка: "Cannot read property 'faculty' of undefined"

**Причина:** req.user не содержит полную информацию о пользователе

**Решение:** Проверьте middleware auth:
```javascript
// В middleware/auth.js должно быть:
const user = await User.findById(decoded.userId);
req.user = {
  userId: user._id,
  role: user.role,
  // Важно: добавить все поля
  faculty: user.faculty,
  program: user.program,
  course: user.course,
  // ... и т.д.
};
```

---

### Ошибка: "Poll.addVote is not a function"

**Причина:** Старая модель Poll все еще в памяти

**Решение:** 
1. Перезапустите сервер
2. Очистите кэш node_modules (если используется кэш)
3. Проверьте, что файл Poll.js обновлен

---

### Ошибка: "Опрос не найден" при запросе аналитики

**Причина:** Неправильный pollId или опрос действительно не существует

**Решение:**
```bash
# Проверить в MongoDB
db.polls.find().pretty()

# Скопировать _id существующего опроса
# Использовать его в GET /api/polls/:pollId/analytics
```

---

## Рекомендации по развертыванию

### В production:

1. **Индексы MongoDB:**
   - Модель автоматически создаст индексы при первом запуске
   - Проверьте, что индексы созданы: `db.polls.getIndexes()`

2. **Кэш аналитики:**
   - При большом количестве голосов обновление кэша может быть медленным
   - Рассмотрите использование очереди (Bull, RabbitMQ) для фонового обновления

3. **Оптимизация:**
   - Для опросов с >1000 голосов кэш аналитики критичен
   - Не пересчитывайте аналитику каждый раз - используйте cached_analytics

---

## Миграция существующих опросов

Если нужно конвертировать старые опросы в новый формат:

```javascript
// server/src/scripts/migratePolls.js

const mongoose = require('mongoose');
const Poll = require('../models/Poll');
require('../config/database');

const migratePolls = async () => {
  try {
    console.log('🔄 Миграция опросов...');
    
    const polls = await Poll.find({ type: { $exists: false } });
    
    for (const poll of polls) {
      // Определяем тип на основе структуры
      if (poll.questions && poll.questions.length > 0) {
        poll.type = 'custom';
      } else {
        poll.type = 'custom';
      }
      
      // Инициализируем пустые массивы для таргетинга
      poll.target_groups = poll.target_groups || [];
      poll.target_faculties = poll.target_faculties || [];
      poll.target_programs = poll.target_programs || [];
      poll.target_courses = poll.target_courses || [];
      
      await poll.save();
      console.log(`✅ Обновлен опрос: ${poll.title}`);
    }
    
    console.log('✅ Миграция завершена!');
    process.exit(0);
  } catch (error) {
    console.error('❌ Ошибка:', error);
    process.exit(1);
  }
};

migratePolls();
```

---

## Тестовые данные

### Быстрое создание тестовых пользователей:

```javascript
// server/src/scripts/createTestUsers.js

const User = require('../models/User');
const bcrypt = require('bcrypt');

const testUsers = [
  {
    email: 'test1@rudn.ru',
    password: 'password123',
    full_name: 'Тестовый Студент 1',
    role: 'student',
    faculty: 'fit',
    faculty_name: 'Факультет информационных технологий',
    program: 'devops',
    program_name: 'DevOps',
    course: 2,
    group: 'ИКБО-01-23',
    group_id: 1234,
    group_name: 'ИКБО-01-23'
  },
  {
    email: 'test2@rudn.ru',
    password: 'password123',
    full_name: 'Тестовый Студент 2',
    role: 'student',
    faculty: 'fit',
    faculty_name: 'Факультет информационных технологий',
    program: 'data-science',
    program_name: 'Data Science',
    course: 3,
    group: 'ИКБО-02-22',
    group_id: 5678,
    group_name: 'ИКБО-02-22'
  },
  {
    email: 'test3@rudn.ru',
    password: 'password123',
    full_name: 'Тестовый Студент 3',
    role: 'student',
    faculty: 'fmeo',
    faculty_name: 'Факультет математики',
    program: 'economics',
    program_name: 'Экономика',
    course: 2,
    group: 'ЭБ-01-23',
    group_id: 9999,
    group_name: 'ЭБ-01-23'
  }
];

const createTestUsers = async () => {
  for (const userData of testUsers) {
    const hashedPassword = await bcrypt.hash(userData.password, 10);
    const user = new User({ ...userData, password: hashedPassword });
    await user.save();
    console.log(`✅ Создан: ${user.email}`);
  }
  
  console.log('✅ Все тестовые пользователи созданы!');
  process.exit(0);
};

createTestUsers();
```

---

## Контрольный чеклист перед тестированием

- [ ] Сервер запущен без ошибок
- [ ] MongoDB подключена
- [ ] Обновлены существующие пользователи (добавлены faculty_name, etc.)
- [ ] Созданы тестовые пользователи с разными метаданными
- [ ] Есть JWT токены для тестирования
- [ ] Создан хотя бы один опрос нового типа
- [ ] Проголосовали несколько пользователей
- [ ] Аналитика возвращает данные без ошибок

---

## Что можно улучшить в будущем

1. **Кэш Redis:** Хранить аналитику в Redis для моментального доступа
2. **Очередь задач:** Bull/RabbitMQ для фонового пересчета аналитики
3. **Webhooks:** Уведомления при достижении порога голосов
4. **Экспорт:** PDF/Excel отчеты с графиками
5. **Сравнение:** Сравнение результатов разных опросов
6. **Тренды:** Отслеживание динамики во времени
7. **AI анализ:** Интеграция с GigaChat для автоматического анализа

---

## Поддержка

Если возникнут проблемы:

1. Проверьте логи сервера
2. Проверьте структуру данных в MongoDB
3. Убедитесь, что все поля заполнены
4. Проверьте права доступа (creator_id, role)

---

**Создано:** 6 января 2025  
**Версия:** 1.0




