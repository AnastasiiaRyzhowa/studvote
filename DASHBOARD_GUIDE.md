# Dashboard StudVote - Руководство

## Что создано

Полноценный Dashboard с навигацией, списком опросов и системой фильтрации.

---

## Структура файлов

```
client/src/
├── components/
│   ├── Layout/
│   │   ├── DashboardLayout.js   ✨ Главный layout с Header + Sidebar
│   │   ├── Header.js             ✨ Шапка с профилем и выходом
│   │   └── Sidebar.js            ✨ Боковая навигация
│   └── Polls/
│       └── PollCard.js           ✨ Карточка опроса
├── pages/
│   ├── DashboardMain.js          ✨ Главная страница со списком опросов
│   ├── CreatePoll.js             ✨ Создание опроса (заглушка)
│   ├── MyPolls.js                ✨ Мои опросы (заглушка)
│   ├── Profile.js                ✨ Профиль (заглушка)
│   └── Leaderboard.js            ✨ Рейтинг (заглушка)
└── App.js                        📝 Обновлен (новые роуты)
```

---

## Компоненты

### 1. DashboardLayout

Главный layout который оборачивает все страницы Dashboard.

**Включает:**
- Header (фиксированный сверху)
- Sidebar (слева, 260px)
- Main content (справа с отступом)

**Использование:**
```jsx
import DashboardLayout from '../components/Layout/DashboardLayout';

<DashboardLayout>
  {/* Ваш контент */}
</DashboardLayout>
```

---

### 2. Header

**Функции:**
- Логотип "StudVote" (клик → главная)
- Баллы и уровень студента (Chip компоненты)
- Имя пользователя
- Аватар с выпадающим меню
  - Профиль
  - Выйти (очищает токен, редирект на /login)

**Данные из:**
- `useAuth()` - получает user, logout

---

### 3. Sidebar

**Навигация:**
- Главная (`/dashboard`)
- Мои опросы (`/my-polls`)
- Создать опрос (`/polls/create`)
- Профиль (`/profile`)
- Рейтинг (`/leaderboard`)

**Особенности:**
- Активная ссылка подсвечивается градиентом
- Иконки Material-UI
- Hover эффекты

---

### 4. PollCard

Карточка для отображения опроса.

**Показывает:**
- Название опроса
- Описание (2 строки с обрезкой)
- Автор с аватаром
- Статус (Активен/Завершен)
- Прогресс-бар голосования
- Дедлайн ("Через X дн.", "Завтра", "Сегодня")
- Кнопка действия:
  - "Голосовать" (если активен и не голосовал)
  - "Посмотреть результаты" (если голосовал)
  - "Результаты" (если завершен)

**Props:**
```javascript
{
  id: number,
  title: string,
  description: string,
  author: string,
  status: 'active' | 'completed' | 'pending',
  deadline: string (ISO date),
  votedCount: number,
  totalStudents: number,
  hasVoted: boolean
}
```

**Эффекты:**
- Hover: поднимается вверх на 4px
- Плавные тени

---

## Страницы

### DashboardMain (`/dashboard`)

**Функции:**
- Фильтры опросов (Tabs):
  - Все
  - Активные
  - Завершенные
  - Мои голоса
- Grid список опросов (3 колонки на desktop)
- Плавающая кнопка "+" (создать опрос)

**Mock данные:**
6 примеров опросов с разными статусами

---

### Заглушки

Все остальные страницы (CreatePoll, Profile, Leaderboard, MyPolls) имеют:
- DashboardLayout
- Иконку
- Заголовок
- Текст "Страница в разработке"

---

## Роутинг

```javascript
/login                  → Login (публичный)
/register               → Register (публичный)
/dashboard              → DashboardMain (защищен)
/polls/create           → CreatePoll (защищен)
/my-polls               → MyPolls (защищен)
/profile                → Profile (защищен)
/leaderboard            → Leaderboard (защищен)
/                       → Redirect → /dashboard
/*                      → Redirect → /dashboard
```

---

## Дизайн

### Цветовая палитра

```javascript
Gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%)
Фон: #F9FAFB
Карточки: white
Текст основной: #111827
Текст вторичный: #6B7280
Текст disabled: #9CA3AF

Статусы:
- Активен: #10B981 (зеленый)
- Завершен: #6B7280 (серый)
- Ожидает: #F59E0B (оранжевый)
```

### Компоненты

- Border radius: 12px-16px
- Shadows: 0 2px 8px rgba(0,0,0,0.08)
- Hover shadows: 0 4px 16px rgba(0,0,0,0.12)
- Transitions: all 0.3s ease

---

## Mock данные

Файл: `pages/DashboardMain.js`

Массив `MOCK_POLLS` содержит 6 примеров:
1. Выбор старосты (активен, 18/25)
2. Новогоднее мероприятие (активен, 22/25, проголосовал)
3. Оценка преподавания (активен, 15/25)
4. Экскурсия (завершен, 24/25)
5. Групповой проект (завершен, 25/25)
6. Факультативный курс (активен, 8/25)

---

## Интеграция с Backend (будущее)

### API endpoints для опросов:

```javascript
GET    /api/polls              // Список всех опросов
GET    /api/polls/:id          // Детали опроса
POST   /api/polls              // Создать опрос
POST   /api/polls/:id/vote     // Проголосовать
GET    /api/polls/:id/results  // Результаты
GET    /api/my-polls           // Мои опросы
```

### Замена mock данных:

В `DashboardMain.js`:
```javascript
// Вместо MOCK_POLLS
const [polls, setPolls] = useState([]);
const [loading, setLoading] = useState(true);

useEffect(() => {
  const fetchPolls = async () => {
    try {
      const response = await api.get('/polls');
      setPolls(response.data.polls);
    } catch (error) {
      console.error(error);
    } finally {
      setLoading(false);
    }
  };
  
  fetchPolls();
}, []);
```

---

## Адаптивность

### Desktop (>960px)
- Sidebar: 260px
- Grid: 3 колонки
- Все элементы видны

### Tablet (600-960px)
- Sidebar: 260px
- Grid: 2 колонки
- Все элементы видны

### Mobile (<600px)
- Sidebar: скрыт (можно добавить Drawer)
- Grid: 1 колонка
- Имя пользователя скрыто в Header

---

## Навигация между страницами

### Из Header:
- Логотип → `/dashboard`
- Профиль → `/profile`
- Выйти → `/login`

### Из Sidebar:
- Все пункты меню

### Из Dashboard:
- Кнопка "+" → `/polls/create`
- Карточка опроса → (в будущем) `/polls/:id`

---

## Состояние аутентификации

**Используется AuthContext:**
```javascript
const { user, logout } = useAuth();

// user содержит:
{
  full_name: string,
  email: string,
  role: 'student' | 'teacher' | 'admin',
  student_data: {
    points: number,
    level: number,
    badges: [],
    streak_days: number
  }
}
```

---

## Проверка работы

1. Запустите приложение: `npm start`
2. Войдите через `/login`
3. Вы попадёте на `/dashboard`
4. Увидите:
   - Header с вашим именем и баллами
   - Sidebar с навигацией
   - 6 карточек опросов
   - Плавающую кнопку "+"

5. Проверьте навигацию:
   - Клики по Sidebar → переходы на страницы-заглушки
   - Клик на "+" → `/polls/create`
   - Клик на аватар → меню → Выйти → `/login`

6. Проверьте фильтры:
   - Все → 6 опросов
   - Активные → 4 опроса
   - Завершенные → 2 опроса
   - Мои голоса → 3 опроса

---

## Следующие шаги

1. Создать backend API для опросов
2. Интегрировать реальные данные вместо mock
3. Добавить страницу детального просмотра опроса
4. Реализовать форму создания опроса
5. Добавить страницу результатов
6. Реализовать голосование
7. Добавить реальный профиль
8. Реализовать рейтинг студентов

---

## Готово!

Dashboard полностью функционален и готов к использованию с mock данными.
При интеграции с backend просто замените mock данные на API запросы.

Все компоненты следуют Material-UI best practices и полностью адаптивны.






























