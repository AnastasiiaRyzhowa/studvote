# Обновление системы голосования

## 📅 Дата: 2026-01-15

## 🎯 Цель обновления
Унификация системы голосования для совместимости с новыми моделями User и Poll, стандартизация формата ответов Q1-Q5 для lesson_review.

---

## 🔧 Изменённые файлы

### 1. **Frontend**

#### `/client/src/components/Polls/NewPollVote.js`

**Изменения:**
- ✅ Добавлено автоматическое преобразование ID вопросов в формат Q1-Q5 для lesson_review
- ✅ Endpoint изменён на унифицированный `/api/polls/:id/vote`
- ✅ Комментарий передаётся отдельным параметром `comment`
- ✅ Добавлена поддержка `technical_issues`

**Маппинг вопросов:**
```javascript
{
  'q1_relevance'    → 'Q1',  // Актуальность материала
  'q2_clarity'      → 'Q2',  // Понятность изложения
  'q3_practice'     → 'Q3',  // Практическая ценность
  'q4_engagement'   → 'Q4',  // Вовлеченность студентов
  'q5_organization' → 'Q5',  // Организация занятия
  'q6_comment'      → 'comment' // Комментарий (отдельно)
}
```

**Формат запроса:**
```javascript
POST /api/polls/:id/vote
{
  "answers": {
    "Q1": 4,
    "Q2": 5,
    "Q3": 3,
    "Q4": 4,
    "Q5": 5
  },
  "comment": "Отличное занятие!",
  "technical_issues": {
    "has_issues": false,
    "selected": [],
    "description": ""
  }
}
```

---

### 2. **Backend**

#### `/server/src/routes/pollRoutes.js`

**Изменения:**
- ✅ Добавлен унифицированный endpoint `POST /:pollId/vote`
- ✅ Изменён порядок роутов (специфичные перед общими)
- ✅ Добавлены алиасы для обратной совместимости
- ✅ Улучшена структура и комментарии

**Структура роутов:**
```javascript
// Специфичные роуты (перед :id и :pollId)
GET  /polls/counts
GET  /polls/my/created
GET  /polls/votes/my
POST /polls/votes
POST /polls/generate-ai
POST /polls/generate-form-ai

// Голосование (перед :id)
POST /polls/:pollId/vote          // ОСНОВНОЙ ENDPOINT
POST /polls/:id/vote-new          // АЛИАС для совместимости

// Аналитика (перед :id)
GET  /polls/:pollId/my-feedback-summary
GET  /polls/:pollId/analytics
GET  /polls/:id/analyze

// Общий роут (В САМОМ КОНЦЕ)
GET  /polls/:id
```

#### `/server/src/controllers/pollController.js`

**Изменения:**
- ✅ `submitVote` теперь поддерживает оба формата параметров: `req.params.id` и `req.params.pollId`
- ✅ Добавлена поддержка параметра `technical_issues`
- ✅ Улучшена валидация ответов для lesson_review
- ✅ Начисление баллов: 10 базовых + 5 за развёрнутый комментарий (>20 символов)

**Логика обработки:**
```javascript
exports.submitVote = async (req, res) => {
  const pollId = req.params.id || req.params.pollId; // Оба формата
  const { answers, comment, technical_issues } = req.body;
  
  // Структурирование ответов для lesson_review (Q1-Q5)
  // Валидация обязательных вопросов
  // Расчёт ИКОП через poll.addVote()
  // Начисление баллов пользователю
  // Синхронизация user.points ↔ user.student_data.points
  // Обновление счётчиков votes_count, polls_participated
}
```

---

## 🔄 Миграция данных

### `/server/scripts/migrateData.js`

**Выполнено:**
- ✅ Синхронизация `user.points` ↔ `user.student_data.points` для всех студентов
- ✅ Инициализация счётчиков `votes_count`, `polls_created_count`, `comments_count`
- ✅ Преобразование ответов в старом формате (массив, q1-q5, числовые ключи) → Q1-Q5
- ✅ Заполнение `student_metadata` в `poll.responses`
- ✅ Пересчёт ИКОП для старых ответов без него

**Результаты миграции:**
```
[OK] Пользователи: обновлено 92, пропущено 7
[OK] Опросы: обновлено 25, пропущено 0
[STATS] Студентов с points: 92
[STATS] Lesson review с discipline_name: 8
[STATS] Опросов со структурированными ответами (Q1-Q5): 8
[DONE] Все проверки пройдены успешно!
```

---

## 📊 Формат данных

### Ответы студента (lesson_review)

**Старый формат (НЕ использовать):**
```javascript
// Массив
[4, 5, 3, 4, 5]

// Объект с маленькими буквами
{ q1: 4, q2: 5, q3: 3, q4: 4, q5: 5 }

// Числовые ключи
{ "1": 4, "2": 5, "3": 3, "4": 4, "5": 5 }
```

**Новый формат (ИСПОЛЬЗОВАТЬ):**
```javascript
{
  "Q1": 4,  // Актуальность материала (1-5)
  "Q2": 5,  // Понятность изложения (1-5)
  "Q3": 3,  // Практическая ценность (1-5)
  "Q4": 4,  // Вовлеченность студентов (1-5)
  "Q5": 5   // Организация занятия (1-5)
}
```

### Response в базе данных

```javascript
{
  "user_id": "507f1f77bcf86cd799439011",
  "answers": {
    "Q1": 4,
    "Q2": 5,
    "Q3": 3,
    "Q4": 4,
    "Q5": 5
  },
  "comment": "Отличное занятие!",
  "ikop": 82,  // Рассчитывается автоматически
  "student_metadata": {
    "faculty": "Экономический факультет",
    "program": "Экономика",
    "course": 2,
    "group": "ЭК-21-1"
  },
  "technical_issues": {
    "has_issues": false,
    "selected": [],
    "description": ""
  },
  "submitted_at": "2026-01-15T10:30:00.000Z"
}
```

---

## 🎮 API Endpoints

### Голосование

#### POST `/api/polls/:id/vote` ✅ ОСНОВНОЙ

**Описание:** Унифицированный endpoint для всех типов опросов

**Поддерживает:**
- lesson_review (с расчётом ИКОП)
- custom опросы
- обычные опросы

**Request:**
```json
{
  "answers": {
    "Q1": 4,
    "Q2": 5,
    "Q3": 3,
    "Q4": 4,
    "Q5": 5
  },
  "comment": "Отличное занятие!",
  "technical_issues": {
    "has_issues": false
  }
}
```

**Response:**
```json
{
  "success": true,
  "message": "Голос учтён",
  "ikop": 82,
  "points_earned": 15,
  "new_total_points": 125
}
```

#### POST `/api/polls/:id/vote-new` ⚠️ DEPRECATED

**Описание:** Алиас для обратной совместимости, использует тот же контроллер `submitVote`

---

## 🏆 Начисление баллов

### Логика начисления:

1. **Базовые баллы:** `10` за любое голосование
2. **Бонус за комментарий:** `+5` если комментарий > 20 символов
3. **Итого:** от `10` до `15` баллов за голосование

### Синхронизация:

```javascript
// Автоматическая двусторонняя синхронизация
user.points = 125;
user.student_data.points = 125; // Синхронизируется автоматически

// При сохранении
await user.save(); // или
await user.addPoints(10, 'Описание');
```

---

## 🧪 Тестирование

### Проверить систему голосования:

1. **Создать lesson_review опрос** (через админку или быстрое создание)
2. **Проголосовать** через форму NewPollVote
3. **Проверить:**
   - ✅ Ответы сохранены в формате Q1-Q5
   - ✅ ИКОП рассчитан правильно
   - ✅ Баллы начислены студенту
   - ✅ user.points === user.student_data.points
   - ✅ votes_count увеличился
   - ✅ poll.total_votes увеличился

### Проверить админ-панель:

1. **Просмотр результатов** `/admin/polls/:id`
2. **Проверить:**
   - ✅ Графики ИКОП отображаются
   - ✅ Распределение по Q1-Q5 корректное
   - ✅ Группа студента отображается
   - ✅ Комментарии видны

---

## 🚨 Важные изменения

### ⚠️ Breaking Changes

1. **Формат ответов lesson_review:**
   - Старый: `{q1, q2, q3, q4, q5}`
   - Новый: `{Q1, Q2, Q3, Q4, Q5}`
   - ✅ Миграция выполнена автоматически

2. **Endpoint голосования:**
   - Старый: `/api/polls/votes` (всё ещё работает)
   - Новый: `/api/polls/:id/vote` (рекомендуется)

3. **Параметры в req.body:**
   - `comment` теперь отдельный параметр (не в `answers`)
   - `technical_issues` добавлен для lesson_review

### ✅ Обратная совместимость

- ✅ Старый endpoint `/api/polls/votes` продолжает работать
- ✅ Контроллер принимает оба формата: `req.params.id` и `req.params.pollId`
- ✅ Миграция обработала все старые данные
- ✅ Компонент NewPollVote автоматически преобразует форматы

---

## 📝 Рекомендации

### Для разработчиков:

1. **Используйте новый endpoint** `/api/polls/:id/vote` для всех новых форм
2. **Формат Q1-Q5** обязателен для lesson_review
3. **Проверяйте синхронизацию** points при работе с User
4. **Используйте методы модели** `user.addPoints()`, `poll.addVote()`

### Для тестирования:

1. **Запустите миграцию** если обновляете существующую БД
2. **Проверьте консоль** на наличие ошибок ИКОП
3. **Протестируйте голосование** для обоих типов опросов
4. **Проверьте админ-панель** на корректность отображения

---

## ✅ Чеклист готовности

- [x] Frontend компонент обновлён (NewPollVote.js)
- [x] Backend роуты обновлены (pollRoutes.js)
- [x] Контроллер обновлён (submitVote)
- [x] Миграция данных выполнена
- [x] Обратная совместимость обеспечена
- [x] Линтер не выдаёт ошибок
- [x] Документация создана

---

## 🔗 Связанные файлы

### Frontend:
- `/client/src/components/Polls/NewPollVote.js` - форма голосования
- `/client/src/pages/PollDetail.js` - страница опроса
- `/client/src/services/pollService.js` - API сервис

### Backend:
- `/server/src/routes/pollRoutes.js` - роуты опросов
- `/server/src/controllers/pollController.js` - контроллер (submitVote)
- `/server/src/models/Poll.js` - модель Poll (addVote)
- `/server/src/models/User.js` - модель User (addPoints)
- `/server/src/services/ikopService.js` - расчёт ИКОП

### Миграция:
- `/server/scripts/migrateData.js` - скрипт миграции

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Версию Node.js (должна быть >= 14)
2. Логи сервера на наличие ошибок
3. Формат данных в запросах (используйте DevTools)
4. Синхронизацию баллов в консоли

**Миграция завершена успешно! Система готова к работе.** 🚀
