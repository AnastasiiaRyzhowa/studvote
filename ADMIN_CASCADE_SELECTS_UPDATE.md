# ✅ Обновление: Каскадные селекты для админа

## 🎯 Проблема решена

**До:** Админ не мог выбрать конкретную группу при создании опроса

**После:** Полноценный каскадный выбор Факультет → Программа → Курс → Группа

---

## 📋 Что изменилось

### ✅ Добавлено:

1. **Каскадная загрузка структуры** (из `/api/directory/structure`)
2. **4 типа целевой аудитории:**
   - 🎓 Конкретная группа (каскад: факультет → программа → курс → группа)
   - 🏛️ Весь факультет (выбор: только факультет)
   - 📚 Вся программа (каскад: факультет → программа)
   - 📖 Весь курс (каскад: факультет → программа → курс)

3. **Автоматический сброс зависимых полей:**
   - Выбрали факультет → сброс программы, курса, группы
   - Выбрали программу → сброс курса, группы
   - Выбрали курс → сброс группы

4. **Disabled состояния:**
   - Программа disabled до выбора факультета
   - Курс disabled до выбора программы
   - Группа disabled до выбора курса

5. **Логирование для отладки:**
   ```javascript
   ✅ Структура загружена: 5 факультетов
   📚 Выбран факультет: ФИТиАБД → 8 программ
   🎓 Выбрана программа: DevOps → 4 курсов
   📖 Выбран курс: 2 → 6 групп
   ```

---

## 🔄 Как работает

### 1️⃣ Загрузка структуры при монтировании

```javascript
useEffect(() => {
  loadStructure(); // GET /api/directory/structure
}, []);
```

Получает всю иерархию одним запросом:
```json
{
  "structure": [
    {
      "id": "1",
      "name": "ФИТиАБД",
      "programs": [
        {
          "id": "devops",
          "name": "DevOps",
          "courses": [
            { "number": 2, "groups": [...] }
          ]
        }
      ]
    }
  ]
}
```

### 2️⃣ Выбор типа аудитории

```jsx
<select name="target_type" value={formData.target_type}>
  <option value="group">Конкретная группа</option>
  <option value="faculty">Весь факультет</option>
  <option value="program">Вся программа</option>
  <option value="course">Весь курс</option>
</select>
```

### 3️⃣ Каскадные селекты для группы

```
┌─────────────────────────────────────────────────────────┐
│ 1️⃣ Факультет                [▼ ФИТиАБД        ] ✅     │
├─────────────────────────────────────────────────────────┤
│ 2️⃣ Программа                [▼ DevOps         ] ✅     │
│                              (disabled до ↑)            │
├─────────────────────────────────────────────────────────┤
│ 3️⃣ Курс                     [▼ 2 курс         ] ✅     │
│                              (disabled до ↑)            │
├─────────────────────────────────────────────────────────┤
│ 4️⃣ Группа                   [▼ 22МО05         ] ✅     │
│                              (disabled до ↑)            │
└─────────────────────────────────────────────────────────┘
```

### 4️⃣ Преобразование в массивы

```javascript
// При отправке формы:
const targetArrays = convertToTargetArrays();

// Для группы:
{
  target_groups: ["12345"],      // ← ID выбранной группы
  target_faculties: [],
  target_programs: [],
  target_courses: []
}

// Для факультета:
{
  target_groups: [],
  target_faculties: ["1"],       // ← ID выбранного факультета
  target_programs: [],
  target_courses: []
}
```

---

## 📊 Схема работы

```
┌────────────────────────────────────────────────────────────┐
│                  СОЗДАНИЕ ОПРОСА АДМИНОМ                   │
└────────────────────────────────────────────────────────────┘

1. Монтирование компонента
   ↓
   GET /api/directory/structure
   ↓
   setStructure(response.data.structure)
   ↓
   Все факультеты доступны в первом селекте

2. Выбор типа аудитории
   ↓
   "Конкретная группа" → показать 4 каскадных селекта
   "Весь факультет" → показать 1 селект
   "Вся программа" → показать 2 селекта
   "Весь курс" → показать 3 селекта

3. Выбор факультета (для типа "group")
   ↓
   handleChange('target_faculty', value)
   ↓
   const faculty = structure.find(f => f.id === value)
   ↓
   setPrograms(faculty.programs)
   ↓
   Сброс: target_program, target_course, target_group
   ↓
   Селект "Программа" enabled

4. Выбор программы
   ↓
   handleChange('target_program', value)
   ↓
   const program = programs.find(p => p.id === value)
   ↓
   setAvailableCourses(program.courses.map(c => c.number))
   ↓
   Сброс: target_course, target_group
   ↓
   Селект "Курс" enabled

5. Выбор курса
   ↓
   handleChange('target_course', value)
   ↓
   const groups = program.courses.find(c => c.number === value).groups
   ↓
   setAvailableGroups(groups)
   ↓
   Сброс: target_group
   ↓
   Селект "Группа" enabled

6. Выбор группы
   ↓
   handleChange('target_group', value)
   ↓
   Форма валидна, можно отправить

7. Отправка формы
   ↓
   convertToTargetArrays()
   ↓
   POST /api/admin/polls
   {
     title, description, type, options,
     target_groups: ["12345"],
     target_faculties: [],
     target_programs: [],
     target_courses: []
   }
   ↓
   Успех → navigate(`/admin/polls/${pollId}`)
```

---

## 🎨 Визуальный дизайн

### Типы аудитории с подсветкой:

```
🎯 Целевая аудитория *
─────────────────────────────────────
[▼ Конкретная группа              ]
─────────────────────────────────────

╔═══════════════════════════════════╗
║  1️⃣ Факультет *                   ║
║  [▼ Выберите факультет    ]       ║
╠═══════════════════════════════════╣
║  2️⃣ Программа *                   ║
║  [  Выберите программу    ] 🔒    ║
╠═══════════════════════════════════╣
║  3️⃣ Курс *                        ║
║  [  Выберите курс         ] 🔒    ║
╠═══════════════════════════════════╣
║  4️⃣ Группа *                      ║
║  [  Выберите группу       ] 🔒    ║
╚═══════════════════════════════════╝

🔒 = disabled
```

После выбора факультета:
```
╔═══════════════════════════════════╗
║  1️⃣ Факультет *                   ║
║  [▼ ФИТиАБД               ] ✅    ║
╠═══════════════════════════════════╣
║  2️⃣ Программа *                   ║
║  [▼ Выберите программу    ] ✅    ║  ← enabled!
╠═══════════════════════════════════╣
║  3️⃣ Курс *                        ║
║  [  Выберите курс         ] 🔒    ║
╠═══════════════════════════════════╣
║  4️⃣ Группа *                      ║
║  [  Выберите группу       ] 🔒    ║
╚═══════════════════════════════════╝
```

---

## 🧪 Тестирование

### Сценарий 1: Создание опроса для группы

1. Зайди как **администратор**
2. Перейди в **Создать опрос**
3. Заполни:
   - Название: "Тестовый опрос для 22МО05"
   - Добавь 2+ варианта ответа
   - Выбери тип аудитории: **"Конкретная группа"**
4. **Последовательно выбери:**
   - Факультет: ФИТиАБД ✅
   - Программа: DevOps ✅ (enabled после факультета)
   - Курс: 2 ✅ (enabled после программы)
   - Группа: 22МО05 ✅ (enabled после курса)
5. Нажми **"Создать опрос"**

**Ожидаемый результат:**
```
✅ Опрос создан
📊 Переход на /admin/polls/:id
📤 Payload:
{
  target_groups: ["12345"],
  target_faculties: [],
  target_programs: [],
  target_courses: []
}
```

### Сценарий 2: Создание опроса для факультета

1. Выбери тип аудитории: **"Весь факультет"**
2. Выбери факультет: ФИТиАБД
3. Создай опрос

**Ожидаемый результат:**
```
✅ Опрос создан
📤 Payload:
{
  target_groups: [],
  target_faculties: ["1"],
  target_programs: [],
  target_courses: []
}
```

### Сценарий 3: Проверка автосброса

1. Выбери: Факультет → Программа → Курс → Группа
2. **Измени факультет**
3. Программа, Курс, Группа должны **сброситься** ✅
4. Селекты Программа, Курс, Группа **disabled** ✅

---

## 🐛 Отладка

### Проблема: Выпадающие списки пустые

**Проверь:**
1. Endpoint `/api/directory/structure` работает:
   ```bash
   curl http://localhost:5000/api/directory/structure
   ```

2. В консоли браузера:
   ```
   ✅ Структура загружена: 5 факультетов
   ```

3. В `structure` есть данные:
   ```javascript
   console.log('structure:', structure);
   // [{id: "1", name: "ФИТиАБД", programs: [...]}, ...]
   ```

### Проблема: Селекты не становятся enabled

**Проверь:**
1. После выбора факультета:
   ```javascript
   console.log('programs:', programs);
   // [{ id: "devops", name: "DevOps", courses: [...] }, ...]
   ```

2. Условие disabled:
   ```jsx
   disabled={!formData.target_faculty}  // ← Должно быть false
   ```

### Проблема: Ошибка при отправке

**Проверь payload:**
```javascript
console.log('📤 Отправка опроса:', payload);
```

**Должно быть:**
```json
{
  "target_groups": ["12345"],
  "target_faculties": [],
  "target_programs": [],
  "target_courses": []
}
```

**НЕ должно быть:**
```json
{
  "target_audience": { "type": "group", "value": "12345" }
}
```

---

## 📝 Изменения в коде

### 1. Новые состояния

```javascript
const [structure, setStructure] = useState([]);
const [programs, setPrograms] = useState([]);
const [availableCourses, setAvailableCourses] = useState([]);
const [availableGroups, setAvailableGroups] = useState([]);
```

### 2. Новые поля formData

```javascript
target_type: 'group',
target_faculty: '',
target_program: '',
target_course: '',
target_group: ''
```

### 3. Функция загрузки структуры

```javascript
const loadStructure = async () => {
  const response = await api.get('/directory/structure');
  setStructure(response.data.structure);
};
```

### 4. Обработка каскадных изменений

```javascript
if (name === 'target_faculty') {
  const faculty = structure.find(f => f.id === value);
  setPrograms(faculty?.programs || []);
  // Сброс нижележащих уровней
}

if (name === 'target_program') {
  const program = programs.find(p => p.id === value);
  setAvailableCourses(program?.courses?.map(c => c.number) || []);
  // Сброс нижележащих уровней
}

if (name === 'target_course') {
  const groups = program?.courses
    ?.find(c => c.number === Number(value))
    ?.groups || [];
  setAvailableGroups(groups);
}
```

### 5. Преобразование в массивы

```javascript
const convertToTargetArrays = () => {
  const result = {
    target_groups: [],
    target_faculties: [],
    target_programs: [],
    target_courses: []
  };
  
  switch (formData.target_type) {
    case 'group':
      result.target_groups = [formData.target_group];
      break;
    case 'faculty':
      result.target_faculties = [formData.target_faculty];
      break;
    // ...
  }
  
  return result;
};
```

---

## ✅ Результат

**До:**
```
❌ Нет выбора группы
❌ Список групп пустой
❌ Невозможно создать опрос для конкретной группы
```

**После:**
```
✅ Полный каскадный выбор
✅ Автоматическая загрузка данных
✅ Disabled состояния предотвращают ошибки
✅ Автосброс при изменении родительского уровня
✅ Правильный формат данных для backend
```

---

Каскадные селекты работают! 🎉
