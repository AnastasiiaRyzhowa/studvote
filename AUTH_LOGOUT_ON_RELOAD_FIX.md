# Исправление: Выброс на страницу входа при обновлении

## 🐛 Проблема

**Симптом:** При обновлении страницы (F5) пользователя выбрасывает на страницу входа, хотя он был авторизован.

---

## 🔍 Причины

Проблема может возникать по нескольким причинам:

### 1. **Backend сервер не запущен** ⚠️ (Самая частая причина!)

Если backend не работает:
```
GET /api/auth/me → Network Error
↓
AuthContext ловит ошибку
↓
removeToken() ← Токен удаляется!
↓
Редирект на /login
```

**Решение:** Запустить backend сервер

```bash
cd server
npm start
```

---

### 2. **Токен истек**

JWT токен имеет срок действия **30 дней** (по умолчанию).

Если токен истек:
```
GET /api/auth/me → 401 "Токен истек"
↓
removeToken() ← Токен удаляется
↓
Редирект на /login
```

**Проверить:** Когда вы последний раз входили в систему?

---

### 3. **Пользователь заблокирован**

```
GET /api/auth/me → 403 "Пользователь заблокирован"
↓
removeToken()
↓
Редирект на /login
```

---

### 4. **JWT_SECRET не совпадает**

Если в `.env` файле изменился `JWT_SECRET`, старые токены станут невалидными.

```
GET /api/auth/me → 401 "Недействительный токен"
↓
removeToken()
↓
Редирект на /login
```

---

### 5. **CORS проблема**

Если frontend и backend на разных доменах/портах, и CORS не настроен правильно.

---

## ✅ Решение

### Исправление 1: Улучшенное логирование

**Файл:** `/client/src/contexts/AuthContext.js`

**Добавлено детальное логирование:**

```javascript
try {
  const userData = await getMe();
  console.log('✅ Аутентификация успешна:', userData.user?.email);
  // ...
} catch (error) {
  console.error('❌ Auth check failed:', error);
  console.error('Статус ошибки:', error.response?.status);
  console.error('Сообщение:', error.response?.data?.message);
  console.error('Полный response:', error.response?.data);
  
  // НЕ удаляем токен, если это просто сетевая ошибка
  if (!error.response) {
    console.warn('⚠️ Сетевая ошибка - токен НЕ удален, попробуйте еще раз');
    setState({
      user: null,
      token,  // Сохраняем токен!
      isAuthenticated: false,
      loading: false,
      tempData: null
    });
  } else {
    // Только если backend ответил с ошибкой - удаляем токен
    console.warn('🗑️ Токен невалидный, удаляем');
    removeToken();
    setState({
      user: null,
      token: null,
      isAuthenticated: false,
      loading: false,
      tempData: null
    });
  }
}
```

**Что изменилось:**
- ✅ Добавлено подробное логирование ошибок
- ✅ Токен НЕ удаляется при сетевых ошибках (backend недоступен)
- ✅ Токен удаляется только если backend ответил с ошибкой 401/403

---

### Исправление 2: Проверка backend

**Как проверить, работает ли backend:**

1. Откройте консоль браузера (F12 → Console)
2. Обновите страницу (F5)
3. Смотрите на логи:

```
🔑 Токен найден, проверяем аутентификацию...
❌ Auth check failed: Error: Network Error
⚠️ Сетевая ошибка - токен НЕ удален, попробуйте еще раз
```

**Если видите "Network Error"** → Backend не запущен!

**Решение:**
```bash
cd server
npm start
```

---

## 🧪 Диагностика

### Шаг 1: Проверьте консоль браузера

После обновления страницы (F5) смотрите на логи:

#### Вариант 1: Backend не запущен
```
🔑 Токен найден, проверяем аутентификацию...
❌ Auth check failed: Error: Network Error
Статус ошибки: undefined
Сообщение: undefined
⚠️ Сетевая ошибка - токен НЕ удален
```

**Решение:** Запустить backend

---

#### Вариант 2: Токен истек
```
🔑 Токен найден, проверяем аутентификацию...
❌ Auth check failed: Error: Request failed with status code 401
Статус ошибки: 401
Сообщение: "Токен истек"
🗑️ Токен невалидный, удаляем
```

**Решение:** Войти заново

---

#### Вариант 3: Пользователь заблокирован
```
🔑 Токен найден, проверяем аутентификацию...
❌ Auth check failed: Error: Request failed with status code 403
Статус ошибки: 403
Сообщение: "Пользователь заблокирован"
🗑️ Токен невалидный, удаляем
```

**Решение:** Связаться с администратором

---

#### Вариант 4: Всё работает
```
🔑 Токен найден, проверяем аутентификацию...
✅ Аутентификация успешна: 216890@edu.fa.ru
```

**Результат:** Остаётесь на странице студента ✅

---

### Шаг 2: Проверьте Network tab

Откройте **DevTools → Network → XHR**

1. Обновите страницу (F5)
2. Найдите запрос `GET /api/auth/me`

**Что смотреть:**

| Статус | Причина | Решение |
|--------|---------|---------|
| **200 OK** | Всё работает ✅ | - |
| **401 Unauthorized** | Токен невалидный/истек | Войти заново |
| **403 Forbidden** | Пользователь заблокирован | Связаться с админом |
| **500 Server Error** | Ошибка на backend | Проверить логи backend |
| **Failed (CORS error)** | CORS не настроен | Настроить CORS |
| **Failed (Network Error)** | Backend не запущен | Запустить backend |

---

### Шаг 3: Проверьте localStorage

Откройте **DevTools → Application → Local Storage → http://localhost:3000**

**Что должно быть:**
```
Key: token
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Если токена нет** → Войдите заново

**Если токен есть, но вас выбрасывает** → Backend проблема или токен невалидный

---

### Шаг 4: Проверьте backend логи

В терминале где запущен backend смотрите на ошибки:

```bash
cd server
npm start
```

**Что искать:**
- `Ошибка в authenticate middleware`
- `ValidationError: User validation failed`
- `JWT_SECRET is not defined`

---

## 📊 Архитектура аутентификации

### Поток при загрузке страницы:

```
1. Пользователь обновляет страницу (F5)
   ↓
2. React приложение загружается
   ↓
3. AuthContext выполняет useEffect
   ↓
4. getToken() → Читает токен из localStorage
   ↓
5. Если токен есть → GET /api/auth/me
   ↓
6. Backend проверяет токен через authenticate middleware
   ↓
   ├─ Токен валидный → 200 OK + userData
   │  ↓
   │  └─ setState({ isAuthenticated: true }) → Остаётся на странице ✅
   │
   ├─ Токен истек → 401 "Токен истек"
   │  ↓
   │  └─ removeToken() → Редирект на /login ❌
   │
   ├─ Токен невалидный → 401 "Недействительный токен"
   │  ↓
   │  └─ removeToken() → Редирект на /login ❌
   │
   └─ Backend недоступен → Network Error
      ↓
      └─ Токен НЕ удаляется (после исправления) → Показать сообщение ⚠️
```

---

## 🔧 Как запустить backend

### Вариант 1: Через npm

```bash
cd server
npm start
```

**Ожидаемый вывод:**
```
🚀 Сервер запущен на порте 4000
✅ MongoDB подключена
✅ Redis подключен
```

---

### Вариант 2: Через nodemon (для разработки)

```bash
cd server
npm run dev
```

---

### Вариант 3: Проверить, запущен ли backend

```bash
curl http://localhost:4000/api/auth/me
```

**Если backend работает:**
```json
{
  "success": false,
  "message": "Токен не предоставлен"
}
```

**Если backend НЕ работает:**
```
curl: (7) Failed to connect to localhost port 4000
```

---

## ✅ Итоговый чек-лист

Если вас выбрасывает при обновлении:

- [ ] Проверьте консоль браузера (F12 → Console)
- [ ] Запущен ли backend? (`cd server && npm start`)
- [ ] Есть ли токен в localStorage? (DevTools → Application)
- [ ] Какой статус у запроса `/api/auth/me`? (DevTools → Network)
- [ ] Есть ли ошибки в логах backend?
- [ ] Давно ли вы входили? (Токен действует 30 дней)
- [ ] Изменялся ли JWT_SECRET в .env?

---

## 🎯 Быстрое решение

**90% случаев - backend не запущен:**

```bash
# Терминал 1: Backend
cd server
npm start

# Терминал 2: Frontend (если тоже не запущен)
cd client
npm start
```

**Если это не помогло:**

1. Откройте консоль браузера (F12)
2. Обновите страницу (F5)
3. Посмотрите на логи с эмодзи (🔑 ✅ ❌)
4. Скопируйте ошибку и проверьте по таблице выше

---

## 🔍 Дополнительная информация

### Где хранится токен?

**localStorage.token** - постоянное хранилище (не удаляется при закрытии браузера)

```javascript
// Сохранить
localStorage.setItem('token', 'eyJhbGci...');

// Получить
const token = localStorage.getItem('token');

// Удалить
localStorage.removeItem('token');
```

---

### Как работает JWT токен?

**Формат:** `header.payload.signature`

**Пример:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.
eyJ1c2VySWQiOiI2NTdhYmMxMjMiLCJyb2xlIjoic3R1ZGVudCIsImlhdCI6MTcwNTk1MTIwMCwiZXhwIjoxNzA4NTQzMjAwfQ.
abc123xyz...
```

**Payload (декодированный):**
```json
{
  "userId": "657abc123",
  "role": "student",
  "iat": 1705951200,  // Время создания
  "exp": 1708543200   // Время истечения (30 дней)
}
```

**Проверить токен:** https://jwt.io

---

## 📝 Резюме

### ДО исправления:
```
Backend недоступен → Network Error
  ↓
removeToken() ← Токен удаляется всегда!
  ↓
Редирект на /login ❌
```

### ПОСЛЕ исправления:
```
Backend недоступен → Network Error
  ↓
Токен НЕ удаляется ✅
  ↓
Показать сообщение: "Сетевая ошибка, попробуйте позже"
  ↓
При следующем обновлении (когда backend заработает) → вход успешен ✅
```

---

Готово! Теперь логирование покажет точную причину проблемы. 🎉
