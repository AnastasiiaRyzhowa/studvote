# 🔧 Исправление ошибки создания опросов

## Проблема

При создании опроса/анкеты возникала ошибка валидации:
```
AxiosError
handleSubmit @ CreateForm.js:505
ошибка валидации
```

## Причина

Новая модель `Poll` требует дополнительные поля, которые не отправлялись из формы:
- `start_date` - дата начала опроса
- `status` - статус опроса
- `target_faculties`, `target_programs`, `target_courses` - массивы для таргетинга

## Что исправлено

### 1. Frontend - CreateForm.js (анкеты)

**Было:**
```javascript
const formData = {
  title: title.trim(),
  description: description.trim(),
  type: 'form',
  questions: [...],
  end_date: endDate,
  visibility,
  is_anonymous: isAnonymous
};
```

**Стало:**
```javascript
const formData = {
  title: title.trim(),
  description: description.trim(),
  type: 'form',
  questions: [...],
  start_date: new Date().toISOString(), // ✅ Добавлено
  end_date: new Date(endDate).toISOString(), // ✅ Конвертировано в ISO
  status: 'active', // ✅ Добавлено
  visibility,
  is_anonymous: isAnonymous,
  // ✅ Добавлены пустые массивы для таргетинга
  target_groups: [],
  target_faculties: [],
  target_programs: [],
  target_courses: []
};
```

### 2. Frontend - CreatePoll.js (обычные опросы)

**Было:**
```javascript
const pollData = {
  title: formData.title.trim(),
  description: formData.description.trim(),
  type: formData.type,
  ...
  target_groups: [...]
};
```

**Стало:**
```javascript
const pollData = {
  title: formData.title.trim(),
  description: formData.description.trim(),
  type: 'custom', // ✅ Явно указан тип
  status: 'active', // ✅ Добавлено
  ...
  target_groups: [...],
  // ✅ Добавлены остальные массивы таргетинга
  target_faculties: [],
  target_programs: [],
  target_courses: []
};
```

### 3. Backend - pollController.js

**Было:**
```javascript
const {
  title,
  description,
  type,
  ...
  target_groups
} = req.body;
```

**Стало:**
```javascript
const {
  title,
  description,
  type,
  ...
  target_groups,
  target_faculties, // ✅ Добавлено
  target_programs,  // ✅ Добавлено
  target_courses,   // ✅ Добавлено
  status           // ✅ Добавлено
} = req.body;
```

**В pollData:**
```javascript
const pollData = {
  ...
  type: type || 'custom', // ✅ Значение по умолчанию
  status: status || 'active', // ✅ Значение по умолчанию
  // Таргетинг
  target_groups: Array.isArray(target_groups) ? target_groups.map(g => g.toString()) : [],
  target_faculties: Array.isArray(target_faculties) ? target_faculties : [], // ✅
  target_programs: Array.isArray(target_programs) ? target_programs : [],   // ✅
  target_courses: Array.isArray(target_courses) ? target_courses : []       // ✅
};
```

---

## Как проверить исправление

### Шаг 1: Перезапустить сервер

```bash
cd server
npm run dev
```

Убедитесь что сервер запустился без ошибок.

### Шаг 2: Перезапустить клиент (если был запущен)

```bash
cd client
npm start
```

Если клиент уже был запущен - обновите страницу в браузере (`Cmd+R` / `Ctrl+R`).

### Шаг 3: Создать анкету

1. Перейдите на страницу создания анкеты:
   ```
   http://localhost:3000/create-form
   ```

2. Заполните форму:
   - **Название:** Тестовая анкета
   - **Описание:** Проверка исправления
   - **Дата окончания:** Любая дата в будущем
   - **Добавьте вопрос:**
     - Текст: "Как вам новая система?"
     - Тип: Текстовый ответ

3. Нажмите **"Создать анкету"**

4. **Результат:** Должно появиться сообщение об успешном создании и редирект на `/my-polls`

### Шаг 4: Создать обычный опрос

1. Перейдите на страницу создания опроса:
   ```
   http://localhost:3000/create-poll
   ```

2. Заполните форму:
   - **Название:** Тестовый опрос
   - **Описание:** Проверка исправления
   - **Тип:** Один вариант ответа
   - **Варианты:**
     - Вариант 1
     - Вариант 2
     - Вариант 3
   - **Даты:** Начало (сейчас), Окончание (через неделю)

3. Нажмите **"Создать опрос"**

4. **Результат:** Должно появиться сообщение об успешном создании

---

## Что теперь работает

✅ Создание анкет (form)  
✅ Создание обычных опросов (custom)  
✅ Правильная валидация на backend  
✅ Все обязательные поля заполняются автоматически  
✅ Совместимость со старыми опросами  

---

## Дополнительные улучшения

### Для анкет:
- Добавлено логирование для отладки: `console.log('Отправка данных формы:', formData)`
- Даты конвертируются в ISO формат для корректной работы с MongoDB

### Для опросов:
- Добавлено логирование: `console.log('Отправка данных опроса:', pollData)`
- Тип явно устанавливается как `'custom'` для обычных опросов

### На backend:
- Добавлены значения по умолчанию: `type: type || 'custom'`
- Добавлена обработка всех полей таргетинга
- `target_groups` конвертируется в строки для совместимости

---

## Если проблема не решена

### 1. Проверьте консоль браузера

Откройте DevTools (F12) → Console и посмотрите полный текст ошибки:
```javascript
console.log('Отправка данных формы:', formData);
```

### 2. Проверьте логи сервера

В терминале где запущен сервер должна быть информация об ошибке:
```
❌ ОШИБКА СОЗДАНИЯ ОПРОСА:
Тип ошибки: ValidationError
Ошибки валидации: {...}
```

### 3. Проверьте MongoDB

Убедитесь что MongoDB запущена:
```bash
# Mac/Linux
brew services list | grep mongodb

# Или
ps aux | grep mongod
```

### 4. Очистите кэш браузера

Иногда помогает:
- Chrome: `Cmd+Shift+Delete` (Mac) / `Ctrl+Shift+Delete` (Windows)
- Выберите "Кэш" и очистите

---

## Структура полей для создания опроса

### Минимальные обязательные поля:

```javascript
{
  title: "Название опроса",
  type: "form" | "custom" | "topic" | "teacher" | "subject" | "organization",
  end_date: "2025-01-15T23:59:59.000Z",
  
  // Для type='form'
  questions: [{
    id: "q1",
    text: "Вопрос?",
    type: "text" | "single" | "multiple" | "rating",
    required: true,
    options: []
  }],
  
  // Для type='custom' (обычные опросы)
  options: [{
    text: "Вариант 1"
  }]
}
```

### Рекомендуемые поля:

```javascript
{
  ...минимальные поля...,
  
  start_date: "2025-01-06T00:00:00.000Z",
  status: "active",
  description: "Описание опроса",
  visibility: "public" | "group" | "faculty",
  is_anonymous: false,
  
  // Таргетинг (можно пустые массивы)
  target_groups: [],
  target_faculties: [],
  target_programs: [],
  target_courses: []
}
```

---

**Дата исправления:** 6 января 2025  
**Статус:** ✅ ИСПРАВЛЕНО  
**Версия:** 1.0




