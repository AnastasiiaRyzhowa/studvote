# 🌱 Seed-скрипт для тестирования аналитики

## Краткая информация

Создан seed-скрипт **seedLessonReviews.js** для генерации тестовых данных специально для проверки **экспорта аналитики**.

---

## 📊 Структуры моделей (актуальные)

### 1. MongoDB подключение

```javascript
// В config/database.js
const MONGODB_URI = process.env.MONGODB_URI; // из .env файла
await mongoose.connect(MONGODB_URI);
```

### 2. User модель - обязательные поля для студента

```javascript
{
  email: String,              // ОБЯЗАТЕЛЬНО, unique
  full_name: String,          // ОБЯЗАТЕЛЬНО
  role: 'student',            // ОБЯЗАТЕЛЬНО
  
  // Академическая информация (ОБЯЗАТЕЛЬНО для студента)
  faculty: String,            // 'ФКН', 'ФЭК'
  faculty_name: String,       // полное название
  program: String,            // 'ПИ', 'ИВТ'
  program_name: String,       // полное название
  course: Number,             // 1-5
  group: String,              // 'ПИ-201'
  group_id: Number,           // 201
  group_name: String,         // 'ПИ-201'
  
  // Геймификация
  student_data: {
    points: Number,           // баллы
    level: Number,            // уровень
    badges: [],               // бейджи
    streak_days: Number       // серия дней
  },
  
  // Дубликаты на верхнем уровне (для админки)
  points: Number,
  level: Number,
  
  // Активность
  votes_count: Number,
  is_active: Boolean
}
```

### 3. Poll модель - lesson_review

```javascript
{
  title: String,                    // "Оценка занятия: Базы данных"
  pollType: 'lesson_review',        // ОБЯЗАТЕЛЬНО для ИКОП
  type: 'form',
  
  // Контекст занятия
  lessonContext: {
    subject: String,                // "Базы данных"
    teacher: String,                // "Петров П.П."
    date: Date,
    time: String,                   // "10:00-11:30"
    topic: String,
    group: String,                  // "ПИ-201"
    groupId: String                 // "201"
  },
  
  // Дубликаты для совместимости с админкой
  subject_name: String,
  discipline_name: String,
  teacher_name: String,
  group_name: String,
  group_id: Number,
  
  // Стандартные 5 вопросов
  questions: [
    { id: 'q1_relevance', text: 'Актуальность материала', type: 'rating' },
    { id: 'q2_clarity', text: 'Понятность изложения', type: 'rating' },
    { id: 'q3_practice', text: 'Практическая ценность', type: 'rating' },
    { id: 'q4_engagement', text: 'Вовлечённость студентов', type: 'rating' },
    { id: 'q5_organization', text: 'Организация занятия', type: 'rating' },
    { id: 'q6_comment', text: 'Комментарий', type: 'text' }
  ],
  
  // Ответы (КЛЮЧЕВОЕ!)
  responses: [{
    user_id: ObjectId,              // ID студента
    
    // Ответы Q1-Q5
    answers: {
      q1_relevance: 5,              // 1-5
      q2_clarity: 4,
      q3_practice: 5,
      q4_engagement: 4,
      q5_organization: 5,
      q6_comment: "Отлично!",
      // Дубликаты для совместимости
      Q1: 5,
      Q2: 4,
      Q3: 5,
      Q4: 4,
      Q5: 5
    },
    
    comment: String,                // комментарий
    ikop: 87,                       // рассчитанный ИКОП (0-100)
    
    // КРИТИЧНО для аналитики! Метаданные студента
    user_faculty: String,
    user_faculty_name: String,
    user_program: String,
    user_program_name: String,
    user_course: Number,
    user_group: String,
    user_group_name: String,
    
    // Дубликат для UI
    student_metadata: {
      faculty: String,
      program: String,
      course: Number,
      group: String
    },
    
    submitted_at: Date
  }],
  
  status: 'active',
  creator_id: ObjectId,             // ID преподавателя
  target_groups: [String],
  target_count: Number              // для расчёта охвата
}
```

### 4. Vote модель - НЕ ИСПОЛЬЗУЕТСЯ для lesson_review!

**Важно**: Модель Vote используется только для старых simple polls (single/multiple/rating).

Для `lesson_review` опросов данные хранятся в **Poll.responses[]**, а не в Vote!

---

## 🎯 Формула расчёта ИКОП

```javascript
// Шаг 1: Средняя оценка по 5 критериям (каждый от 1 до 5)
const avgRating = (Q1 + Q2 + Q3 + Q4 + Q5) / 5;

// Шаг 2: Нормализация в диапазон 0-100%
const ikop = ((avgRating - 1) / 4) * 100;

// Примеры:
// Q1=5, Q2=5, Q3=5, Q4=5, Q5=5 → avgRating=5.0 → ИКОП=100%
// Q1=4, Q2=4, Q3=4, Q4=4, Q5=4 → avgRating=4.0 → ИКОП=75%
// Q1=3, Q2=3, Q3=3, Q4=3, Q5=3 → avgRating=3.0 → ИКОП=50%
// Q1=2, Q2=2, Q3=2, Q4=2, Q5=2 → avgRating=2.0 → ИКОП=25%
// Q1=1, Q2=1, Q3=1, Q4=1, Q5=1 → avgRating=1.0 → ИКОП=0%
```

---

## 🚀 Использование seed-скрипта

### Запуск

```bash
cd server
npm run seed-lesson-reviews
```

### Что создаётся

**Факультеты и программы**:
- **ФКН** (Факультет компьютерных наук)
  - ПИ (Программная инженерия)
  - ИВТ (Информатика и вычислительная техника)
  - ИБ (Информационная безопасность)
- **ФЭК** (Факультет экономики)
  - ЭК (Экономика)
  - МЕН (Менеджмент)

**Дисциплины**:
- **ФКН**: Базы данных, Алгоритмы, Веб-программирование, ML, Системное программирование
- **ФЭК**: Микроэкономика, Макроэкономика, Финансы и кредит

**Данные**:
- ~8 преподавателей
- ~200-300 студентов (15-25 на группу)
- ~60-70 опросов lesson_review
- ~1500-2000 ответов с Q1-Q5 и ИКОП

**Распределение оценок** (реалистичное):
- 50% - оценка 5 (отлично)
- 30% - оценка 4 (хорошо)
- 15% - оценка 3 (средне)
- 3% - оценка 2 (плохо)
- 2% - оценка 1 (очень плохо)

**Откликаемость**: 70-90% студентов отвечают на каждый опрос

---

## 📊 Проверка экспорта

### После запуска seed-скрипта:

1. **Войдите как администратор** в систему

2. **Перейдите**: Дашборд качества образования (`/admin/quality-dashboard`)

3. **Настройте фильтры** (опционально):
   - Факультет: ФКН
   - Программа: ПИ
   - Курс: 2
   - Дисциплина: Базы данных

4. **Нажмите**: "Экспортировать" → "Excel (сырые данные)"

5. **Откройте файл** `export_<timestamp>.xlsx`

### Что должно быть в Excel:

**Лист "ИКОП по дисциплинам"**:

| Дисциплина | Кол-во оценок | Q1 | Q2 | Q3 | Q4 | Q5 | Средняя оценка | ИКОП |
|------------|---------------|----|----|----|----|----|----|------|
| Базы данных | 45 | 4.33 | 4.22 | 4.11 | 4.33 | 4.44 | 4.29 | 82.2 |
| Алгоритмы | 38 | 4.05 | 4.18 | 3.95 | 4.11 | 4.26 | 4.11 | 77.8 |
| ... | ... | ... | ... | ... | ... | ... | ... | ... |

### Проверка PDF:

6. **Нажмите**: "Экспортировать" → "PDF (детальный отчет)"

7. **Откройте** `report_detailed_<timestamp>.pdf`

Должен содержать:
- Применённые фильтры
- Общую статистику (всего оценок, средний ИКОП)
- Средние оценки Q1-Q5
- Расшифровку ИКОП и интерпретацию значений

---

## 🔧 Технические детали

### Путь к моделям
```
/server/src/models/
  - User.js
  - Poll.js
  - Vote.js (НЕ используется для lesson_review!)
```

### Путь к скриптам
```
/server/src/scripts/
  - seedLessonReviews.js    (НОВЫЙ - для аналитики)
  - seedAnalytics.js         (старый - другие типы опросов)
  - addRealData.js           (для профиля студента)
  - addTestData.js           (устаревший)
  - cleanTestData.js         (очистка)
```

### Конфигурация MongoDB
```bash
# В .env файле:
MONGODB_URI=mongodb://localhost:27017/studvote
# или
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/studvote
```

### Проверка подключения
```bash
cd server
node -e "require('dotenv').config(); console.log('MONGODB_URI:', process.env.MONGODB_URI)"
```

---

## 🧹 Очистка данных

### Удалить только демо-опросы

Скрипт автоматически удаляет старые демо-опросы при повторном запуске:

```javascript
await Poll.deleteMany({ title: /^Оценка занятия:/ });
```

### Полная очистка (осторожно!)

```javascript
// Удалить ВСЕ опросы (для разработки)
await Poll.deleteMany({});

// Удалить ВСЕ пользователей (для разработки)
await User.deleteMany({});
```

---

## ❓ FAQ

### Q: Можно ли изменить количество студентов?

Да, в seedLessonReviews.js найдите:
```javascript
const studentCount = 15 + Math.floor(Math.random() * 11); // 15-25
```
Измените на нужное число.

### Q: Как изменить факультеты/дисциплины?

Отредактируйте массивы `faculties` и `disciplines` в начале файла seedLessonReviews.js.

### Q: Почему не используется модель Vote?

Vote - устаревшая модель для simple polls. Для lesson_review данные хранятся в Poll.responses[], так как:
1. Нужны метаданные студента (faculty, program, course, group)
2. Нужен расчёт ИКОП
3. Нужны Q1-Q5 ответы, а не option_ids

### Q: Откуда берётся ИКОП в responses?

Рассчитывается в seed-скрипте функцией `calculateIKOP(q1, q2, q3, q4, q5)`.

В реальной системе ИКОП рассчитывается при голосовании (в pollController.js).

### Q: Можно ли запустить несколько раз?

Да, скрипт удаляет старые демо-опросы перед созданием новых. Студенты и преподаватели обновляются (upsert).

### Q: Как проверить что данные созданы?

```bash
# Запустите mongo shell
mongosh studvote

# Проверьте опросы
db.polls.find({ pollType: 'lesson_review' }).count()

# Проверьте ответы
db.polls.aggregate([
  { $match: { pollType: 'lesson_review' } },
  { $project: { responsesCount: { $size: '$responses' } } }
])
```

---

## 📝 Примечания

1. **Структуры моделей проверены** и актуальны на 2026-01-15
2. **Vote НЕ используется** для lesson_review (это критично!)
3. **Метаданные student_metadata обязательны** для аналитики
4. **ИКОП рассчитывается** по формуле `((avgRating - 1) / 4) * 100`
5. **Данные реалистичны**: больше хороших оценок, комментарии зависят от оценок

---

## ✅ Чек-лист запуска

- [ ] Убедитесь, что MongoDB запущен
- [ ] Проверьте .env файл (MONGODB_URI)
- [ ] Запустите: `npm run seed-lesson-reviews`
- [ ] Дождитесь завершения (~10-30 секунд)
- [ ] Войдите как администратор
- [ ] Перейдите в Дашборд качества образования
- [ ] Попробуйте экспорт в Excel
- [ ] Попробуйте экспорт в PDF
- [ ] Проверьте данные в файлах

---

**Дата создания**: 2026-01-15  
**Версия**: 1.0.0  
**Статус**: Готово к использованию ✅
