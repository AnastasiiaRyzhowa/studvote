# Тестирование исправления таргетирования опросов

## Подготовка

1. Убедитесь, что сервер запущен:
```bash
cd server
npm start
```

2. Убедитесь, что клиент запущен:
```bash
cd client
npm start
```

## Сценарии тестирования

### 1. Создание опроса для конкретного факультета

**Шаги:**
1. Войдите как администратор
2. Перейдите в раздел создания опроса (`/admin/polls/create`)
3. Заполните форму:
   - Название: "Опрос для ФИТиАБД"
   - Тип вопроса: "Один вариант ответа"
   - Варианты: "Да", "Нет"
   - **Целевая аудитория: Выберите "Факультет" → выберите "ФИТиАБД" из выпадающего списка**
4. Нажмите "Создать опрос"

**Ожидаемый результат:**
- В консоли браузера увидите: `📤 Отправка опроса администратором: { ... target_faculties: ["ФИТиАБД"] ... }`
- В консоли сервера увидите: `📊 Создание опроса администратором: { ... visibility: "faculty", target_faculties: ["ФИТиАБД"], targetCount: <число> ... }`
- Опрос успешно создан
- Переход на страницу детали опроса `/admin/polls/:id`

**Проверка в БД:**
```bash
# Подключитесь к MongoDB
mongosh
use studvote

# Найдите созданный опрос
db.polls.findOne({ title: "Опрос для ФИТиАБД" })

# Убедитесь, что:
# - visibility: "faculty"
# - target_faculties: ["ФИТиАБД"]
# - target_groups: []
# - target_programs: []
# - target_courses: []
```

---

### 2. Создание опроса для конкретной группы

**Шаги:**
1. Войдите как администратор
2. Перейдите в раздел создания опроса
3. Заполните форму:
   - Название: "Опрос для группы 22МО05"
   - Тип вопроса: "Несколько вариантов"
   - Варианты: "Вариант 1", "Вариант 2", "Вариант 3"
   - **Целевая аудитория: Выберите "Конкретная группа" → выберите группу из выпадающего списка**
4. Нажмите "Создать опрос"

**Ожидаемый результат:**
- В консоли браузера: `📤 Отправка опроса администратором: { ... target_groups: ["22МО05"] ... }`
- В консоли сервера: `📊 Создание опроса администратором: { ... visibility: "group", target_groups: ["22МО05"], targetCount: <число> ... }`
- Опрос успешно создан

**Проверка в БД:**
```javascript
db.polls.findOne({ title: "Опрос для группы 22МО05" })
// Должно быть:
// visibility: "group"
// target_groups: ["22МО05"]
```

---

### 3. Создание опроса для программы обучения

**Шаги:**
1. Войдите как администратор
2. Создайте опрос с выбором **"Программа"**
3. Выберите программу из выпадающего списка

**Ожидаемый результат:**
- `visibility: "program"`
- `target_programs: ["<выбранная программа>"]`

---

### 4. Создание опроса для курса

**Шаги:**
1. Войдите как администратор
2. Создайте опрос с выбором **"Курс"**
3. Выберите курс (1, 2, 3, или 4)

**Ожидаемый результат:**
- `visibility: "course"`
- `target_courses: [<номер курса>]`

---

### 5. Создание публичного опроса (для всех)

**Шаги:**
1. Войдите как администратор
2. Создайте опрос с выбором **"Все студенты"**

**Ожидаемый результат:**
- `visibility: "public"`
- Все массивы таргетирования пустые

---

## Проверка видимости опросов

### Для студента из ФИТиАБД:

1. Войдите как студент факультета ФИТиАБД
2. Перейдите в Dashboard
3. Опросы с `target_faculties: ["ФИТиАБД"]` должны быть видны
4. Опросы с `target_faculties: ["ФМЭО"]` НЕ должны быть видны

### Для студента конкретной группы:

1. Войдите как студент группы 22МО05
2. Опросы с `target_groups: ["22МО05"]` должны быть видны
3. Опросы с `target_groups: ["22МО06"]` НЕ должны быть видны

---

## Проверка данных формы (form-options)

**Тест:**
1. Откройте страницу создания опроса администратором
2. Откройте DevTools → Network
3. Найдите запрос к `/api/admin/form-options`

**Ожидаемый результат:**
```json
{
  "success": true,
  "groups": ["22МО05", "22МО06", ...],
  "faculties": ["ФИТиАБД", "ФМЭО", ...],
  "programs": ["DevOps", "Data Science", ...],
  "disciplines": [...],
  "courses": [1, 2, 3, 4, 5]
}
```

**В консоли сервера:**
```
📋 Опции форм администратора: {
  groups: <количество>,
  faculties: <количество>,
  programs: <количество>,
  disciplines: <количество>
}
```

---

## Возможные проблемы и решения

### 1. Выпадающие списки пустые

**Причина:** Нет студентов с заполненными полями faculty/group/program

**Решение:**
```javascript
// Добавьте тестовых студентов с данными:
db.users.insertOne({
  email: "test@student.hse.ru",
  role: "student",
  faculty: "ФИТиАБД",
  group: "22МО05",
  program: "DevOps",
  course: 2,
  is_active: true,
  // ... другие поля
})
```

### 2. Ошибка "target_audience is required"

**Причина:** Старый формат все еще используется

**Решение:** Убедитесь, что CreatePoll.jsx обновлен и функция `convertTargetAudienceToArrays` присутствует

### 3. Опрос не виден студентам

**Причина:** Неправильное поле group в БД

**Решение:** Проверьте, что в модели User используется правильное поле:
```javascript
// В Poll.js метод isVisibleTo проверяет:
this.target_groups.includes(user.group_id.toString())

// Убедитесь, что в User есть:
// - group (название группы)
// - group_id (ID группы из RUZ)
```

---

## Чек-лист успешного исправления

- [ ] Выпадающие списки загружаются (groups, faculties, programs)
- [ ] При выборе факультета → отправляется `target_faculties: ["..."]`
- [ ] При выборе группы → отправляется `target_groups: ["..."]`
- [ ] В БД создается опрос с правильным `visibility`
- [ ] В БД правильно заполнены массивы таргетирования
- [ ] В логах видны 📤 и 📊 сообщения
- [ ] Студенты видят только те опросы, которые им предназначены
- [ ] `max_responses` вычисляется корректно

---

## Дополнительная отладка

### Включите детальные логи в Chrome DevTools:

1. Откройте Console
2. Создайте опрос
3. Проверьте лог `📤 Отправка опроса администратором:`
4. Убедитесь, что структура данных правильная

### Проверьте логи сервера:

```bash
# В терминале где запущен сервер должны появиться:
📋 Опции форм администратора: { groups: 10, faculties: 3, ... }
📤 Отправка опроса администратором: { ... }
📊 Создание опроса администратором: { ... }
```

### MongoDB запросы для отладки:

```javascript
// Найти все опросы администратора
db.polls.find({ creator_role: "admin" }).pretty()

// Найти опросы с таргетированием на факультет
db.polls.find({ target_faculties: { $ne: [] } }).pretty()

// Проверить студентов с заполненными данными
db.users.find({ 
  role: "student", 
  is_active: true,
  faculty: { $exists: true, $ne: null }
}).count()
```

---

Удачного тестирования! 🚀
