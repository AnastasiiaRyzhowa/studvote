# AI Анализ Результатов - Быстрый Старт

## ✅ Что было добавлено

### Backend (Серверная часть)

1. **Контроллер**: `server/src/controllers/pollController.js`
   - Функция `analyzeResults` - обрабатывает запросы на анализ

2. **Роут**: `server/src/routes/pollRoutes.js`
   - `GET /api/polls/:id/analyze` - endpoint для получения анализа

### Frontend (Клиентская часть)

3. **Сервис**: `client/src/services/pollService.js`
   - Функция `analyzePollResults(pollId)` - вызывает API

---

## 🚀 Как использовать

### 1. Из клиентского кода

```javascript
import { analyzePollResults } from '../services/pollService';

// Получить анализ
const analysis = await analyzePollResults('pollId');
console.log(analysis.analysis);
```

### 2. Прямой API запрос

```bash
GET http://localhost:5000/api/polls/[POLL_ID]/analyze
Authorization: Bearer [YOUR_JWT_TOKEN]
```

### 3. Пример ответа

```json
{
  "success": true,
  "analysis": "1. ЛИДЕР: Вариант А с 65% голосов\n2. АКТИВНОСТЬ: 200 участников\n3. ИНСАЙТ: Явное предпочтение..."
}
```

---

## 📋 Требования

- ✅ Опрос должен существовать в БД
- ✅ Пользователь должен быть авторизован (JWT)
- ✅ `GIGACHAT_AUTH_KEY` должен быть настроен в `.env`

---

## 🔍 Что анализирует AI

1. **ЛИДЕР** - какой вариант победил и с каким перевесом
2. **АКТИВНОСТЬ** - насколько активно участвовали студенты
3. **ИНСАЙТ** - главные выводы и рекомендации

---

## ⚡ Быстрый тест

### Шаг 1: Запустите сервер

```bash
cd server
npm start
```

### Шаг 2: Получите токен

```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "user@example.com", "password": "password"}'
```

### Шаг 3: Запросите анализ

```bash
curl http://localhost:5000/api/polls/YOUR_POLL_ID/analyze \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 💡 Интеграция в UI

### Простой пример кнопки

```javascript
const AnalyzeButton = ({ pollId }) => {
  const [analysis, setAnalysis] = useState('');
  const [loading, setLoading] = useState(false);

  const handleClick = async () => {
    setLoading(true);
    try {
      const result = await analyzePollResults(pollId);
      setAnalysis(result.analysis);
    } catch (error) {
      console.error('Ошибка:', error);
    } finally {
      setLoading(false);
    }
  };

  return (
    <>
      <button onClick={handleClick} disabled={loading}>
        {loading ? 'Анализирую...' : 'Анализировать с AI'}
      </button>
      {analysis && <pre>{analysis}</pre>}
    </>
  );
};
```

---

## 📖 Полная документация

Смотрите `AI_ANALYSIS_GUIDE.md` для подробной информации.

---

## ❓ Частые вопросы

**Q: Сколько времени занимает анализ?**  
A: Обычно 2-5 секунд, зависит от GigaChat API.

**Q: Нужно ли создавать опрос специальным образом?**  
A: Нет, работает с любыми опросами в системе.

**Q: Можно ли анализировать опрос без голосов?**  
A: Да, но анализ будет отражать отсутствие активности.

**Q: Требуется ли быть создателем опроса?**  
A: Нет, любой авторизованный пользователь может запросить анализ.

---

## 🐛 Возможные ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| 404 | Опрос не найден | Проверьте ID опроса |
| 401 | Не авторизован | Добавьте JWT токен |
| 500 | Ошибка AI | Проверьте GigaChat настройки |

---

Готово! 🎉 Теперь можно анализировать результаты опросов с помощью AI.























