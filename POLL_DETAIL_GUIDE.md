# PollDetail - Страница голосования и результатов

## Описание

Страница для просмотра деталей опроса, голосования и просмотра результатов.

**Файл:** `client/src/pages/PollDetail.js`

**URL:** `/polls/:id`

---

## Функциональность

### Основные возможности

1. **Просмотр информации об опросе**
   - Название, описание
   - Автор опроса
   - Статус (Активен/Завершен)
   - Время до окончания
   - Количество проголосовавших

2. **Голосование** (если не голосовал)
   - Выбор варианта/вариантов
   - Отправка голоса
   - Получение баллов

3. **Просмотр результатов** (если проголосовал)
   - Графики с процентами
   - Информация о собственном голосе
   - Количество баллов

---

## Структура страницы

### 1. Header опроса (всегда видно)

```
┌─────────────────────────────────────┐
│ [<- Назад к списку]                 │
│                                     │
│ [Завершен] [Осталось: 7 дн. 5 ч.]  │
│                                     │
│ Выбор старосты группы ПИ-401        │ <- Название
│ Голосование за старосту на          │
│ следующий семестр                   │ <- Описание
│ ─────────────────────────────────── │
│ [👤] Автор: Иванова А.И.            │
│      Проголосовало: 15 человек      │
│      Тип: Один вариант              │
└─────────────────────────────────────┘
```

### 2. Форма голосования (если has_voted = false)

**Для type = 'single':**
```
┌─────────────────────────────────────┐
│ [🗳️] Выберите вариант               │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ ○ Иванов Иван                   │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ ● Петрова Анна                  │ │ <- Выбран
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ ○ Сидоров Петр                  │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [       Проголосовать       ]       │ <- Кнопка
└─────────────────────────────────────┘
```

**Для type = 'multiple':**
```
┌─────────────────────────────────────┐
│ [🗳️] Выберите вариант               │
│ ℹ️ Выберите до 2 вариантов          │ <- Подсказка
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ ☑ Кафе "Шоколадница"            │ │ <- Выбран
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ ☑ Боулинг "Космик"              │ │ <- Выбран
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ ☐ Каток в Сокольниках           │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [       Проголосовать       ]       │
└─────────────────────────────────────┘
```

**Для type = 'rating':**
```
┌─────────────────────────────────────┐
│ [🗳️] Выберите вариант               │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │     Отлично (5)                 │ │
│ │     ★★★★★                       │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │     Хорошо (4)                  │ │
│ │     ★★★★☆                       │ │ <- Выбран
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │     Удовлетворительно (3)       │ │
│ │     ★★★☆☆                       │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [       Проголосовать       ]       │
└─────────────────────────────────────┘
```

### 3. Результаты (если has_voted = true)

```
┌─────────────────────────────────────┐
│ ✓ Ваш голос учтён!                  │
│ Вы проголосовали за: Петрова Анна   │
│ 🏆 Заработано: +10 баллов           │
│ ─────────────────────────────────── │
│ Результаты голосования              │
│                                     │
│ Иванов Иван                         │
│ ████████░░░░░░░░░░ 40%       6      │
│                                     │
│ Петрова Анна                        │
│ ████████████████░░ 60%       9      │
│                                     │
│ Сидоров Петр                        │
│ ░░░░░░░░░░░░░░░░░░  0%       0      │
│ ─────────────────────────────────── │
│ Всего проголосовало:     15 человек │
└─────────────────────────────────────┘
```

---

## Состояния страницы

### Loading (загрузка)

```javascript
if (loading) {
  return <CircularProgress />;
}
```

Показывается спиннер в центре страницы.

### Error (опрос не найден)

```javascript
if (!poll) {
  return (
    <Alert severity="error">
      Опрос не найден
    </Alert>
    <Button>Вернуться к списку</Button>
  );
}
```

### Доступен для голосования

**Условия:**
- `poll.status === 'active'`
- `poll.has_voted === false`

Показывается **форма голосования**.

### Результаты

**Условия:**
- `poll.has_voted === true` (пользователь проголосовал)
- ИЛИ `poll.status === 'completed'` (опрос завершен)

Показываются **результаты с графиками**.

---

## Логика голосования

### Валидация

```javascript
const handleVote = async () => {
  // 1. Проверка: выбран хотя бы один вариант
  if (selectedOptions.length === 0) {
    setError('Выберите хотя бы один вариант');
    return;
  }
  
  // 2. Проверка для single: только один вариант
  if (poll.type === 'single' && selectedOptions.length > 1) {
    setError('Можно выбрать только один вариант');
    return;
  }
  
  // 3. Проверка max_choices для multiple
  if (poll.max_choices && selectedOptions.length > poll.max_choices) {
    setError(`Максимум ${poll.max_choices} вариантов`);
    return;
  }
  
  // 4. Отправка голоса
  const result = await vote(poll._id, selectedOptions);
  
  // 5. Успех! Показываем результаты
  setVoteSuccess({
    points: result.points_earned,
    message: 'Ваш голос учтён!',
    votedOptions: selectedOptions.map(i => poll.options[i].text)
  });
  
  // 6. Обновляем данные опроса
  const updated = await getPollById(poll._id);
  setPoll(updated.poll);
  setShowResults(true);
};
```

### Отправка в API

```javascript
POST /api/polls/votes
{
  poll_id: "675abc...",
  option_ids: [0, 2] // индексы выбранных вариантов
}

// Ответ:
{
  success: true,
  vote: { ... },
  points_earned: 10
}
```

---

## Обработка разных типов опроса

### Type: single (Radio buttons)

```javascript
<RadioGroup value={selectedOptions[0]} onChange={handleRadioChange}>
  {poll.options.map((option, index) => (
    <Paper onClick={() => setSelectedOptions([index])}>
      <FormControlLabel
        value={index}
        control={<Radio />}
        label={option.text}
      />
    </Paper>
  ))}
</RadioGroup>
```

**Поведение:**
- Можно выбрать только один вариант
- При клике на новый вариант - предыдущий сбрасывается
- `selectedOptions = [index]` (массив с одним элементом)

### Type: multiple (Checkboxes)

```javascript
{poll.options.map((option, index) => (
  <Paper onClick={() => handleCheckboxChange(index, !selectedOptions.includes(index))}>
    <FormControlLabel
      control={
        <Checkbox
          checked={selectedOptions.includes(index)}
          onChange={(e) => handleCheckboxChange(index, e.target.checked)}
        />
      }
      label={option.text}
    />
  </Paper>
))}
```

**Поведение:**
- Можно выбрать несколько вариантов
- При клике - добавляется/удаляется из массива
- `selectedOptions = [0, 2, 4]` (массив индексов)
- Валидация: не больше `max_choices`

### Type: rating (Stars/Options)

```javascript
{poll.options.map((option, index) => (
  <Paper onClick={() => setSelectedOptions([index])}>
    <Typography>{option.text}</Typography>
    <Rating
      value={index + 1}
      max={poll.options.length}
      readOnly
    />
  </Paper>
))}
```

**Поведение:**
- Выбор одного варианта из списка рейтингов
- Показываются звезды для визуализации
- `selectedOptions = [index]` (массив с одним элементом)

---

## Визуализация результатов

### Прогресс-бары

```javascript
{poll.options.map((option, index) => {
  const percentage = poll.total_votes > 0
    ? Math.round((option.votes / poll.total_votes) * 100)
    : 0;
  
  return (
    <Box>
      <Typography>{option.text}</Typography>
      <LinearProgress
        variant="determinate"
        value={percentage}
      />
      <Typography>{percentage}% ({option.votes})</Typography>
    </Box>
  );
})}
```

**Стилизация:**
- Градиентный прогресс-бар
- Высота: 10px
- Закругленные углы
- Процент справа

### Информация о голосе

```javascript
{poll.has_voted && voteSuccess && (
  <Box sx={{ bgcolor: '#F0FDF4' }}>
    <CheckCircleIcon /> Ваш голос учтён!
    Вы проголосовали за: {voteSuccess.votedOptions.join(', ')}
    🏆 Заработано: +{voteSuccess.points} баллов
  </Box>
)}
```

**Цвета:**
- Зеленый фон: `#F0FDF4`
- Зеленая граница: `#86EFAC`
- Зеленый текст: `#10B981`

---

## Дополнительные функции

### Таймер до конца опроса

```javascript
const getTimeRemaining = () => {
  const now = new Date();
  const end = new Date(poll.end_date);
  const diff = end - now;
  
  if (diff <= 0) return 'Завершен';
  
  const days = Math.floor(diff / (1000 * 60 * 60 * 24));
  const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
  
  if (days > 0) {
    return `Осталось: ${days} дн. ${hours} ч.`;
  }
  return `Осталось: ${hours} ч.`;
};
```

**Примеры:**
- `Осталось: 7 дн. 5 ч.`
- `Осталось: 23 ч.`
- `Завершен`

### Статус badges

```javascript
<Chip
  label={isCompleted ? 'Завершен' : 'Активен'}
  color={isCompleted ? 'default' : 'success'}
/>

{!isCompleted && (
  <Chip
    icon={<TimerIcon />}
    label={getTimeRemaining()}
  />
)}
```

### Snackbar уведомление

```javascript
<Snackbar open={snackbarOpen} autoHideDuration={5000}>
  <Alert severity="success" icon={<CheckCircleIcon />}>
    Ваш голос учтён! Заработано +{voteSuccess?.points} баллов
  </Alert>
</Snackbar>
```

**Показывается:**
- После успешного голосования
- Внизу по центру
- Автоматически закрывается через 5 сек
- Зеленый цвет

---

## Обработка особых случаев

### Опрос завершен, не успел проголосовать

```javascript
{isCompleted && !poll.has_voted && (
  <Alert severity="warning">
    Опрос завершен. Вы не успели проголосовать.
  </Alert>
)}
```

Показываются только результаты без возможности голосования.

### Опрос не найден

```javascript
if (!poll) {
  return (
    <Alert severity="error">
      Опрос не найден
    </Alert>
  );
}
```

### Ошибка загрузки

```javascript
if (error) {
  return (
    <Alert severity="error">
      Не удалось загрузить опрос
    </Alert>
  );
}
```

---

## Навигация

### Из Dashboard в PollDetail

**PollCard.js:**
```javascript
const handleNavigateToPoll = () => {
  navigate(`/polls/${poll.id}`);
};

<Button onClick={handleNavigateToPoll}>
  Голосовать
</Button>
```

### Назад в Dashboard

```javascript
<Button
  startIcon={<ArrowBackIcon />}
  onClick={() => navigate('/dashboard')}
>
  Назад к списку
</Button>
```

---

## Роутинг

**App.js:**
```javascript
<Route
  path="/polls/:id"
  element={
    <ProtectedRoute>
      <PollDetail />
    </ProtectedRoute>
  }
/>
```

**Использование:**
```javascript
const { id } = useParams(); // получить ID из URL
const navigate = useNavigate(); // для навигации
```

---

## Workflow использования

```
1. Пользователь на Dashboard
   ↓
2. Кликает на "Голосовать" в PollCard
   ↓
3. navigate(`/polls/${poll.id}`)
   ↓
4. PollDetail загружается
   - Loading spinner
   - getPollById(id)
   ↓
5. Проверка has_voted:
   
   НЕТ → Показать форму голосования
         ↓
         Выбрать варианты
         ↓
         Нажать "Проголосовать"
         ↓
         Валидация
         ↓
         POST /api/polls/votes
         ↓
         Успех! Snackbar "Ваш голос учтён! +10 баллов"
         ↓
         Обновить данные (getPollById)
         ↓
         Показать результаты
   
   ДА → Показать результаты сразу
```

---

## Тестирование

### Тест 1: Голосование (single)

1. Откройте Dashboard
2. Найдите активный опрос (не голосовали)
3. Кликните "Голосовать"
4. Выберите один вариант
5. Нажмите "Проголосовать"
6. ✅ Snackbar "Ваш голос учтён! +10 баллов"
7. ✅ Показываются результаты с графиками
8. ✅ Видно ваш выбор

### Тест 2: Голосование (multiple)

1. Откройте опрос с типом "multiple"
2. ℹ️ Появится подсказка "Выберите до N вариантов"
3. Выберите несколько вариантов
4. Нажмите "Проголосовать"
5. ✅ Успех

**Тест 2.1: Превышение max_choices**
- Выберите больше вариантов чем max_choices
- Нажмите "Проголосовать"
- ❌ Ошибка "Максимум N вариантов"

### Тест 3: Просмотр результатов

1. Откройте опрос, в котором уже голосовали
2. ✅ Сразу показываются результаты
3. ✅ Виден зеленый блок "Ваш голос учтён!"
4. ✅ Прогресс-бары с процентами

### Тест 4: Таймер

1. Откройте активный опрос
2. ✅ Badge "Осталось: X дн. Y ч."
3. Откройте завершенный опрос
4. ✅ Badge "Завершен"

### Тест 5: Навигация

1. На странице опроса нажмите "Назад к списку"
2. ✅ Редирект на /dashboard

---

## Интеграция с API

### Загрузка опроса

```javascript
GET /api/polls/:id

// Ответ:
{
  success: true,
  poll: {
    _id: "675abc...",
    title: "...",
    description: "...",
    type: "single",
    options: [
      { text: "...", votes: 5 },
      { text: "...", votes: 3 }
    ],
    total_votes: 8,
    has_voted: false,
    status: "active",
    start_date: "...",
    end_date: "...",
    creator_id: {
      full_name: "Иванова А.И."
    }
  }
}
```

### Голосование

```javascript
POST /api/polls/votes
{
  poll_id: "675abc...",
  option_ids: [0, 2]
}

// Ответ:
{
  success: true,
  vote: { ... },
  points_earned: 10
}
```

---

## Готово!

Страница PollDetail полностью функциональна:

- ✅ Загрузка данных опроса
- ✅ Форма голосования для 3 типов (single/multiple/rating)
- ✅ Валидация выбора
- ✅ Отправка голоса в API
- ✅ Результаты с графиками
- ✅ Snackbar уведомления
- ✅ Таймер до окончания
- ✅ Навигация из/в Dashboard
- ✅ Обработка всех состояний (loading, error, empty)
- ✅ Адаптивный дизайн
- ✅ Красивый UI

Теперь пользователи могут голосовать в опросах и видеть результаты!


























