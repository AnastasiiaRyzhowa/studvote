# 🤖 Генерация анкет с помощью AI

## ✅ СТАТУС: РЕАЛИЗОВАНО

Добавлена возможность генерации анкет (форм) с помощью GigaChat AI на странице создания анкеты.

---

## 🎯 ФУНКЦИОНАЛЬНОСТЬ

### Что делает AI:
- Генерирует **название анкеты** на основе темы
- Создает **описание анкеты**
- Формирует **3-7 вопросов** разных типов:
  - 📝 **Единичный выбор** (radio buttons)
  - ☑️ **Множественный выбор** (checkboxes)
  - ⭐ **Рейтинг** (1-5 звезд)
  - ✍️ **Свободный ответ** (текстовое поле)
- Автоматически добавляет **варианты ответов** для вопросов с выбором
- Отмечает **важные вопросы** как обязательные

---

## 💻 РЕАЛИЗАЦИЯ

### Backend

#### 1. **GigaChatService** (`/server/src/services/gigachatService.js`)

Добавлен новый метод `generateForm()`:

```javascript
async generateForm(prompt) {
  // Генерирует структуру анкеты:
  // {
  //   title: "Название анкеты",
  //   description: "Описание",
  //   questions: [
  //     {
  //       id: "q1",
  //       text: "Текст вопроса",
  //       type: "single|multiple|rating|text",
  //       required: true/false,
  //       options: ["Вариант 1", "Вариант 2"]
  //     }
  //   ]
  // }
}
```

**Особенности:**
- Валидация и нормализация вопросов
- Автоматическая генерация ID для вопросов
- Ограничение до 15 вопросов максимум
- Ограничение до 10 вариантов ответа на вопрос
- Умная обработка типов вопросов (rating и text без options)

#### 2. **PollController** (`/server/src/controllers/pollController.js`)

Добавлен endpoint `generateFormWithAI`:

```javascript
exports.generateFormWithAI = async (req, res) => {
  const { prompt } = req.body;
  const formData = await gigachatService.generateForm(prompt);
  
  res.json({
    success: true,
    form: formData
  });
};
```

#### 3. **Routes** (`/server/src/routes/pollRoutes.js`)

Новый route:
```javascript
POST /api/polls/generate-form-ai
```

**Требования:**
- Аутентификация обязательна
- Body: `{ prompt: "описание темы" }`

---

### Frontend

#### 1. **PollService** (`/client/src/services/pollService.js`)

Добавлен метод:

```javascript
export const generateFormWithAI = async (prompt) => {
  const response = await api.post('/polls/generate-form-ai', { prompt });
  return response.data;
};
```

#### 2. **CreateForm** (`/client/src/pages/CreateForm.js`)

**Добавленные элементы:**

1. **Кнопка "Создать с AI"**
   - Расположена в заголовке рядом с названием страницы
   - Градиентный фиолетово-синий дизайн
   - Иконка `auto_awesome`

2. **Модальное окно генерации**
   - Поле для ввода описания темы
   - Информационная подсказка о возможностях AI
   - Кнопки "Сгенерировать" и "Отмена"
   - Индикатор загрузки во время генерации
   - Блокировка интерфейса при загрузке

3. **Логика заполнения формы**
   ```javascript
   const handleAIGenerate = async () => {
     const response = await generateFormWithAI(aiPrompt);
     
     // Заполняем поля формы
     setTitle(response.form.title);
     setDescription(response.form.description);
     setQuestions(response.form.questions);
   };
   ```

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ

### Для пользователя:

1. Откройте страницу **"Создание анкеты"**
2. Нажмите кнопку **"Создать с AI"** в правом верхнем углу
3. В появившемся окне введите описание темы анкеты:
   ```
   Например: Анкета удовлетворенности студентов качеством обучения
   ```
4. Нажмите **"Сгенерировать"**
5. Дождитесь загрузки (5-10 секунд)
6. AI заполнит:
   - ✅ Название анкеты
   - ✅ Описание
   - ✅ Список вопросов с вариантами ответов
7. Отредактируйте при необходимости:
   - Измените текст вопросов
   - Добавьте/удалите варианты ответов
   - Измените типы вопросов
   - Добавьте новые вопросы
8. Настройте параметры (дата окончания, видимость)
9. Нажмите **"Создать анкету"**

---

## 📊 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Анкета для студентов

**Промпт:**
```
Опрос студентов о качестве питания в столовой
```

**Результат AI:**
```json
{
  "title": "Оценка качества питания в студенческой столовой",
  "description": "Помогите нам улучшить сервис и качество блюд",
  "questions": [
    {
      "text": "Как часто вы посещаете столовую?",
      "type": "single",
      "required": true,
      "options": ["Каждый день", "2-3 раза в неделю", "Редко", "Никогда"]
    },
    {
      "text": "Оцените качество блюд",
      "type": "rating",
      "required": true,
      "options": []
    },
    {
      "text": "Какие блюда вы бы хотели видеть в меню?",
      "type": "multiple",
      "required": false,
      "options": ["Салаты", "Супы", "Горячие блюда", "Десерты", "Напитки"]
    },
    {
      "text": "Ваши пожелания и комментарии",
      "type": "text",
      "required": false,
      "options": []
    }
  ]
}
```

### Пример 2: Анкета для мероприятия

**Промпт:**
```
Регистрация на студенческую конференцию
```

**Результат AI:**
- Вопросы о ФИО и контактах
- Выбор секции участия
- Формат участия (очно/онлайн)
- Дополнительные пожелания

### Пример 3: Обратная связь

**Промпт:**
```
Анкета обратной связи после лекции
```

**Результат AI:**
- Оценка качества материала
- Понятность изложения
- Интересные темы
- Предложения по улучшению

---

## 🎨 UI/UX ОСОБЕННОСТИ

### Дизайн модального окна:
- ✨ **Градиентный заголовок** (фиолетово-синий)
- 🌙 **Поддержка темной темы**
- 📱 **Адаптивный дизайн**
- 🔄 **Анимированный индикатор загрузки**
- ❌ **Защита от случайного закрытия** во время генерации

### Пользовательский опыт:
- Быстрая генерация (5-10 секунд)
- Можно редактировать результат
- Понятные подсказки
- Обработка ошибок с понятными сообщениями

---

## ⚡ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Промпт для GigaChat:

```javascript
const userPrompt = `Создай анкету на тему: ${prompt}.

Верни JSON в формате:
{
  "title": "название анкеты",
  "description": "краткое описание анкеты",
  "questions": [...]
}

Требования:
1. Создай 3-7 вопросов разных типов
2. Типы: "single", "multiple", "rating", "text"
3. Для single и multiple добавь 3-5 вариантов
4. Для rating и text options = []
5. Отмечай важные как required: true

Отвечай ТОЛЬКО JSON.`;
```

### Валидация на backend:
- ✅ Проверка структуры ответа
- ✅ Нормализация типов вопросов
- ✅ Генерация уникальных ID
- ✅ Ограничение количества вопросов (до 15)
- ✅ Ограничение вариантов ответа (до 10)

### Обработка ошибок:
```javascript
// Frontend
try {
  const response = await generateFormWithAI(prompt);
  // Заполнение формы
} catch (error) {
  setError('Не удалось сгенерировать анкету');
}

// Backend
if (!formData || !formData.questions) {
  return res.status(500).json({
    success: false,
    message: 'Не удалось сгенерировать анкету'
  });
}
```

---

## 🔧 API REFERENCE

### POST /api/polls/generate-form-ai

**Request:**
```json
{
  "prompt": "Описание темы анкеты"
}
```

**Response (Success):**
```json
{
  "success": true,
  "form": {
    "title": "Название анкеты",
    "description": "Описание анкеты",
    "questions": [
      {
        "id": "q1",
        "text": "Текст вопроса",
        "type": "single",
        "required": true,
        "options": ["Вариант 1", "Вариант 2"]
      }
    ]
  }
}
```

**Response (Error):**
```json
{
  "success": false,
  "message": "Не удалось сгенерировать анкету"
}
```

**Headers:**
- `Authorization: Bearer <token>` - обязательно

---

## 📝 РЕКОМЕНДАЦИИ ПО ИСПОЛЬЗОВАНИЮ

### Хорошие промпты:
✅ "Анкета удовлетворенности студентов качеством обучения"
✅ "Регистрация на студенческий хакатон"
✅ "Опрос о предпочтениях в выборе факультативов"
✅ "Обратная связь после мастер-класса"

### Плохие промпты:
❌ "анкета" (слишком общее)
❌ "создай форму" (нет контекста)
❌ "опрос про все" (нет фокуса)

### Советы:
1. **Будьте конкретны** - укажите тему и контекст
2. **Добавляйте детали** - для кого анкета, зачем нужна
3. **Редактируйте результат** - AI дает хорошую основу, но вы можете улучшить
4. **Проверяйте вопросы** - убедитесь, что они логичны

---

## 🎉 ПРЕИМУЩЕСТВА

### Для пользователей:
- ⚡ **Экономия времени** - создание анкеты за 10 секунд вместо 10 минут
- 🎯 **Профессиональные вопросы** - AI формулирует логичные вопросы
- 🔄 **Разнообразие типов** - автоматически используются разные типы вопросов
- ✏️ **Полная свобода** - можно редактировать результат

### Для проекта:
- 🚀 **Улучшение UX** - упрощает создание анкет
- 💡 **Инновационность** - использование AI выделяет проект
- 📈 **Вовлеченность** - пользователи чаще создают анкеты

---

## 📦 ИЗМЕНЕННЫЕ ФАЙЛЫ

### Backend:
- ✅ `/server/src/services/gigachatService.js` - метод `generateForm()`
- ✅ `/server/src/controllers/pollController.js` - endpoint `generateFormWithAI`
- ✅ `/server/src/routes/pollRoutes.js` - route `POST /api/polls/generate-form-ai`

### Frontend:
- ✅ `/client/src/services/pollService.js` - метод `generateFormWithAI()`
- ✅ `/client/src/pages/CreateForm.js` - UI для AI генерации

---

## 🧪 ТЕСТИРОВАНИЕ

### Сценарий 1: Успешная генерация
1. Откройте страницу создания анкеты
2. Нажмите "Создать с AI"
3. Введите: "Опрос студентов о качестве общежития"
4. Нажмите "Сгенерировать"
5. ✅ Форма должна заполниться данными
6. ✅ Должно быть 3-7 вопросов
7. ✅ Вопросы разных типов

### Сценарий 2: Редактирование результата
1. Сгенерируйте анкету с AI
2. Измените текст любого вопроса
3. Добавьте новый вопрос
4. Удалите один из вариантов ответа
5. ✅ Все изменения должны сохраниться

### Сценарий 3: Ошибка сети
1. Отключите интернет
2. Попробуйте сгенерировать анкету
3. ✅ Должно появиться сообщение об ошибке
4. ✅ Форма не должна поломаться

---

## 🔮 БУДУЩИЕ УЛУЧШЕНИЯ

### Возможные доработки:
1. **Примеры промптов** - кнопки с готовыми темами
2. **Настройка количества вопросов** - выбор 3, 5 или 7 вопросов
3. **Выбор языка** - генерация на разных языках
4. **История генераций** - сохранение предыдущих результатов
5. **Регенерация отдельных вопросов** - изменить только один вопрос
6. **Предпросмотр** - показать как будет выглядеть анкета для респондентов

---

**Дата реализации:** 23 декабря 2025  
**Статус:** Production Ready ✅  
**Версия:** 1.0


















