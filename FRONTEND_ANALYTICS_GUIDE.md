# 🎨 Фронтенд аналитики опросов - Руководство

## ✅ Что реализовано

### 1. Компонент AnalyticsDashboard

**Путь:** `client/src/components/Analytics/AnalyticsDashboard.js`

Красивый компонент для отображения аналитики с:

#### Ключевые выводы (Инсайты)
- 🏆 Лидеры среди факультетов/программ
- 📊 Большая разница между группами
- 📈📉 Тренды по курсам
- ✅⚠️ Оценка явки студентов

#### Срезы по факультетам
- Отображение среднего балла по каждому факультету
- Прогресс-бары с анимацией
- Количество ответов

#### Срезы по программам
- Карточки с названием программы и факультета
- Средний балл крупным шрифтом
- Количество студентов
- Hover эффекты

#### Срезы по курсам
- Цветные карточки для каждого курса
- Градиентный фон
- Средний балл или количество ответов

#### Общая статистика
- Всего ответов
- Средний балл
- Распределение оценок (1-5 звезд)

---

### 2. Обновлена страница PollResults

**Путь:** `client/src/pages/PollResults.js`

**Добавлено:**

#### Новая кнопка "Показать аналитику срезов"
```javascript
<button onClick={loadAnalytics}>
  <span>Показать аналитику срезов</span>
</button>
```

#### Интеграция компонента AnalyticsDashboard
```javascript
{showAnalytics && analytics && (
  <AnalyticsDashboard 
    analytics={analytics} 
    pollType={poll.type} 
  />
)}
```

#### Функция загрузки аналитики
```javascript
const loadAnalytics = async () => {
  const response = await getPollAnalytics(poll._id);
  setAnalytics(response.analytics);
  setShowAnalytics(true);
};
```

---

### 3. Обновлен сервис pollService

**Путь:** `client/src/services/pollService.js`

**Добавлена функция:**
```javascript
export const getPollAnalytics = async (pollId) => {
  const response = await api.get(`/polls/${pollId}/analytics`);
  return response.data;
};
```

---

## 🎯 Как это работает

### Сценарий использования:

1. **Пользователь открывает страницу результатов опроса**
   - URL: `/polls/:id/results`

2. **Видит основные метрики:**
   - Всего голосов
   - Активность %
   - Осталось дней
   - Статус

3. **Нажимает кнопку "Показать аналитику срезов"**
   - Отправляется запрос: `GET /api/polls/:pollId/analytics`
   - Проверяются права доступа (только создатель или админ)

4. **Загружается красивая аналитика:**
   - Ключевые выводы с иконками 🏆📊📈
   - Срезы по факультетам с прогресс-барами
   - Срезы по программам в карточках
   - Срезы по курсам с градиентами
   - Общая статистика

---

## 🎨 Дизайн системы

### Цветовая палитра:

```css
/* Основные цвета */
primary: #0167FC
emerald: #10b981 (для программ)
orange: #f97316 (для курсов)
purple: #8b5cf6 (для статистики)

/* Градиенты */
from-primary to-blue-600
from-blue-50 to-indigo-50
from-orange-500 to-orange-700
```

### Анимации:

- `hover:scale-105` - увеличение при наведении
- `hover:-translate-y-1` - поднятие карточки
- `transition-all duration-1000` - плавные переходы
- `hover:shadow-[0_0_10px_#0167FC]` - свечение

### Иконки (Material Symbols):

- `lightbulb` - инсайты
- `school` - факультеты
- `code` - программы
- `stairs` - курсы
- `analytics` - статистика
- `star` - оценки

---

## 📊 Структура данных аналитики

### Формат ответа от API:

```json
{
  "success": true,
  "analytics": {
    "overall": {
      "total_responses": 5,
      "average": 4.2,
      "distribution": { "1": 0, "2": 0, "3": 1, "4": 2, "5": 2 }
    },
    "byFaculty": {
      "fit": {
        "name": "Факультет информационных технологий",
        "count": 3,
        "average": 4.3
      }
    },
    "byProgram": {
      "devops": {
        "name": "DevOps",
        "faculty": "Факультет информационных технологий",
        "count": 2,
        "average": 4.5
      }
    },
    "byCourse": {
      "2": { "count": 3, "average": 4.3 },
      "3": { "count": 2, "average": 4.0 }
    },
    "insights": [
      {
        "type": "leader",
        "icon": "🏆",
        "text": "Самая высокая оценка у Факультет информационных технологий: 4.3 ⭐"
      }
    ],
    "poll_type": "topic"
  }
}
```

---

## 🚀 Примеры использования

### Пример 1: Опрос для оценки темы

**Backend (создание опроса):**
```json
POST /api/polls
{
  "title": "Оценка темы: Базы данных",
  "type": "topic",
  "questions": [{
    "id": "q1",
    "text": "Насколько понятно?",
    "type": "rating_1_5"
  }],
  "target_faculties": ["fit"],
  "end_date": "2025-01-15T23:59:59Z"
}
```

**Frontend (отображение аналитики):**
- Откройте `/polls/:id/results`
- Нажмите "Показать аналитику срезов"
- Увидите красивые карточки с результатами

---

### Пример 2: Опрос для оценки преподавателя

**Backend:**
```json
POST /api/polls
{
  "type": "teacher",
  "teacher_id": "67890",
  "teacher_name": "Иванов И.И.",
  "questions": [
    { "id": "q1", "text": "Доступность объяснения", "type": "rating_1_5" },
    { "id": "q2", "text": "Организация занятий", "type": "rating_1_5" }
  ]
}
```

**Frontend:**
- Аналитика покажет средний балл по каждому вопросу
- Срезы покажут как оценивают разные факультеты/программы/курсы
- Инсайты покажут кто лидирует и есть ли тренды

---

## 🔧 Кастомизация

### Изменить цвета:

**В AnalyticsDashboard.js:**
```javascript
// Для факультетов
from-primary to-blue-600 → from-purple-500 to-purple-700

// Для программ
text-emerald-600 → text-blue-600

// Для курсов
from-orange-500 to-orange-700 → from-pink-500 to-pink-700
```

### Добавить новые срезы:

**В AnalyticsDashboard.js:**
```javascript
{/* Срез по группам */}
{byGroup && Object.keys(byGroup).length > 0 && (
  <div className="bg-white dark:bg-card-dark rounded-3xl shadow-soft p-8">
    <h3>Результаты по группам</h3>
    {Object.entries(byGroup).map(([key, data]) => (
      <div key={key}>
        {data.name}: {data.average}
      </div>
    ))}
  </div>
)}
```

### Изменить размер карточек:

```javascript
// Было
grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5

// Стало (меньше карточек в ряд)
grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3
```

---

## 📱 Адаптивность

### Брейкпоинты:

- **Mobile:** 1 колонка
- **Tablet (md):** 2 колонки
- **Desktop (lg):** 4-5 колонок

### Пример:

```javascript
// Инсайты
grid-cols-1 md:grid-cols-2

// Программы
grid-cols-1 md:grid-cols-2

// Курсы
grid-cols-2 md:grid-cols-4 lg:grid-cols-5
```

---

## ⚠️ Важные замечания

### 1. Права доступа

Аналитику могут видеть только:
- Создатель опроса
- Админы

Если пользователь не имеет прав - кнопка будет, но при клике ничего не загрузится (ошибка в консоли).

### 2. Пустые данные

Если нет ответов - компонент ничего не отобразит (return null).

### 3. Типы опросов

Аналитика работает для:
- `topic` - оценка темы
- `teacher` - оценка преподавателя
- `subject` - оценка дисциплины
- `organization` - организационные опросы

Для `form` (анкеты) - аналитика не поддерживается (кнопка скрыта).

### 4. Производительность

- Аналитика загружается отдельно (не при загрузке страницы)
- Используется кэш на backend для быстрого доступа
- Компонент легковесный и не нагружает страницу

---

## 🐛 Возможные проблемы

### Проблема: Кнопка не работает

**Решение:**
```javascript
// Проверьте в консоли браузера
console.error('Ошибка загрузки аналитики:', error);

// Возможные причины:
// 1. Нет прав доступа (не создатель)
// 2. Опрос не найден
// 3. Backend не запущен
```

### Проблема: Аналитика пустая

**Решение:**
- Убедитесь, что есть голоса с метаданными
- Проверьте, что пользователи имеют заполненные поля faculty, program, course
- Старые голоса (без метаданных) не учитываются

### Проблема: Неправильные цвета

**Решение:**
```javascript
// Убедитесь, что Tailwind CSS скомпилирован
npm run build

// Или в dev режиме
npm start
```

---

## 🎯 Следующие шаги

### Что можно добавить:

1. **Экспорт аналитики:**
   - Добавить кнопку "Экспорт аналитики в PDF"
   - Включить срезы в Excel экспорт

2. **Фильтры:**
   - Фильтр по датам
   - Фильтр по конкретному факультету
   - Сравнение с предыдущими опросами

3. **Графики:**
   - Добавить charts.js для визуализации
   - Круговые диаграммы для распределения по факультетам
   - Столбчатые графики для сравнения курсов

4. **Детализация:**
   - Клик на факультет → показать программы этого факультета
   - Клик на программу → показать группы

---

## 📚 Полезные ресурсы

- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Material Symbols](https://fonts.google.com/icons)
- [Recharts Documentation](https://recharts.org/)
- [React Documentation](https://react.dev/)

---

## 🔗 Связанные файлы

### Backend:
- `server/src/models/Poll.js` - модель опроса
- `server/src/services/analyticsService.js` - сервис аналитики
- `server/src/controllers/pollController.js` - контроллер (getPollAnalytics)
- `server/src/routes/pollRoutes.js` - роуты

### Frontend:
- `client/src/services/pollService.js` - API сервис
- `client/src/components/Analytics/AnalyticsDashboard.js` - компонент аналитики
- `client/src/pages/PollResults.js` - страница результатов

---

**Создано:** 6 января 2025  
**Версия:** 1.0  
**Автор:** Cursor AI  
**Проект:** StudVote - Платформа студенческого самоуправления




