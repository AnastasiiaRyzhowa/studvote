# Реализация экспорта данных - Сводка изменений

## ✅ Что реализовано

### 1. POST /api/admin/export/excel
**Файл**: `/server/src/routes/adminRoutes.js` (строки ~402-520)

**Функционал**:
- ✅ Экспорт аналитики ИКОП по дисциплинам (`type: 'analytics'` или `'quality'`)
- ✅ Экспорт списка студентов (`type: 'users'`)
- ✅ Поддержка фильтров (факультет, программа, дисциплина, даты)
- ✅ Автоматическая ширина колонок
- ✅ Обработка ошибок (нет данных → 404)

**Данные для экспорта ИКОП**:
- Дисциплина
- Количество оценок
- Средние оценки по 5 критериям (Q1-Q5)
- Общая средняя оценка
- Рассчитанный ИКОП (0-100%)

**Данные для экспорта студентов**:
- ФИО, Email, Группа, Факультет, Программа, Курс
- Баллы, Уровень, Количество голосований

---

### 2. POST /api/admin/export/pdf/:type
**Файл**: `/server/src/routes/adminRoutes.js` (строки ~522-640)

**Функционал**:
- ✅ Сводный отчёт (`type: 'summary'`)
- ✅ Детальный отчёт (`type: 'detailed'`)
- ✅ Поддержка фильтров
- ✅ Красивое форматирование PDF

**Содержание сводного отчёта**:
- Применённые фильтры
- Общая статистика (всего оценок, опросов, средний ИКОП)
- Средние оценки по 5 критериям

**Дополнительно в детальном отчёте**:
- Расшифровка показателя ИКОП
- Интерпретация значений (80-100%, 60-79%, 40-59%, 0-39%)
- Описание 5 критериев оценки

---

### 3. Уже работающие endpoints

#### POST /api/admin/export/custom-polls
**Файл**: `/server/src/routes/adminRoutes.js` (строки ~443-608)
- ✅ Экспорт списка свободных опросов
- ✅ Два листа Excel: данные + статистика

#### POST /api/admin/polls/:id/export  
**Файл**: `/server/src/routes/adminRoutes.js` (строки ~879-900)
- ✅ Экспорт результатов конкретного опроса
- ✅ Форматы: Excel и PDF

---

## 🎨 Frontend интеграция

### Компоненты с кнопками экспорта

#### 1. QualityDashboard.jsx (строки 145-173, 437-473)
**Кнопка**: "Экспортировать"
**Dropdown меню**:
- Excel (сырые данные) → `/admin/export/excel`
- PDF (сводный отчет) → `/admin/export/pdf/summary`
- PDF (детальный отчет) → `/admin/export/pdf/detailed`

**Передаваемые данные**:
```javascript
{
  type: 'analytics',
  filters: {
    faculty: filters.faculty,
    program: filters.program,
    course: filters.course,
    group: filters.group,
    discipline: filters.discipline,
    teacher: filters.teacher,
    period: filters.period,
    dateFrom: filters.dateFrom,
    dateTo: filters.dateTo
  }
}
```

#### 2. PollResults.jsx (строки 60-84, 188-205)
**Кнопки**: 
- "Экспорт в Excel"
- "Экспорт в PDF"

**Endpoint**: `/admin/polls/${id}/export`
**Передаваемые данные**: `{ format: 'xlsx' | 'pdf' }`

#### 3. CustomPollsDashboard (QualityDashboard.jsx, строки 590-614)
**Кнопка**: "Экспорт списка"
**Endpoint**: `/admin/export/custom-polls`
**Передаваемые данные**:
```javascript
{
  filters: {
    categoryFilter: '...',
    statusFilter: '...',
    creatorFilter: '...'
  }
}
```

---

## 📊 Структура данных

### ИКОП (Индекс Качества Образовательного Процесса)

**Формула расчёта**:
```javascript
const avgRating = (Q1 + Q2 + Q3 + Q4 + Q5) / 5; // Средняя оценка (1-5)
const ikop = ((avgRating - 1) / 4) * 100;       // Нормализация в 0-100%
```

**Интерпретация**:
- 🟢 80-100%: Отличное качество
- 🟡 60-79%: Хорошее качество
- 🟠 40-59%: Требует внимания
- 🔴 0-39%: Критическое состояние

**5 критериев** (Q1-Q5):
1. Актуальность материала
2. Понятность изложения
3. Практическая ценность
4. Вовлечённость студентов
5. Организация занятия

---

## 🔧 Технические детали

### Используемые библиотеки
```json
{
  "xlsx": "^0.18.5",     // Создание Excel файлов
  "pdfkit": "^0.17.2"    // Создание PDF файлов
}
```

### Middleware
```javascript
router.post('/export/excel', authenticate, isAdmin, async (req, res) => { ... });
router.post('/export/pdf/:type', authenticate, isAdmin, async (req, res) => { ... });
```

### Модели данных
- `Poll` - опросы с ответами (responses)
- `User` - пользователи (студенты)
- `Vote` - голоса (устаревшая модель, не используется в новой реализации)

### Обработка данных

**Excel**:
1. Получение данных из MongoDB
2. Агрегация и группировка
3. Расчёт средних значений и ИКОП
4. Форматирование в JSON
5. Конвертация в XLSX через `XLSX.utils.json_to_sheet()`
6. Отправка как бинарный stream

**PDF**:
1. Создание PDF документа через `PDFDocument`
2. Добавление текста и форматирование
3. Pipe в response stream
4. Автоматическое закрытие при `doc.end()`

---

## 🧪 Тестирование

### Ручное тестирование через Postman/Insomnia

#### 1. Экспорт аналитики в Excel
```http
POST http://localhost:5000/api/admin/export/excel
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

{
  "type": "analytics",
  "filters": {
    "faculty": "ФКН",
    "dateFrom": "2024-09-01",
    "dateTo": "2024-12-31"
  }
}
```

#### 2. Экспорт в PDF (сводный)
```http
POST http://localhost:5000/api/admin/export/pdf/summary
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

{
  "filters": {
    "faculty": "ФКН",
    "discipline": "Базы данных"
  }
}
```

#### 3. Экспорт студентов
```http
POST http://localhost:5000/api/admin/export/excel
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

{
  "type": "users"
}
```

### Проверка через UI

1. Войти как администратор
2. Перейти в **Дашборд качества образования** (`/admin/quality-dashboard`)
3. Настроить фильтры (факультет, программа, дисциплина)
4. Нажать кнопку **"Экспортировать"**
5. Выбрать формат:
   - Excel (сырые данные)
   - PDF (сводный отчет)
   - PDF (детальный отчет)
6. Файл должен скачаться автоматически

---

## 📝 Известные ограничения

1. **PDF для опросов**: Экспортируются только первые 50 ответов (для оптимизации размера)
2. **Фильтры**: Значение `"all"` игнорируется (= без фильтрации)
3. **Кириллица в PDF**: Использует стандартный шрифт (может быть проблема с отображением)
4. **Размер данных**: Для очень больших выборок (>10000 записей) может быть медленно

---

## 🚀 Дальнейшие улучшения

### Приоритет 1 (Важно)
- [ ] Добавить прогресс-бар для больших экспортов
- [ ] Кеширование результатов для повторных запросов
- [ ] Валидация фильтров на backend

### Приоритет 2 (Желательно)
- [ ] Экспорт в CSV формат
- [ ] Поддержка кириллицы в PDF (кастомный шрифт)
- [ ] Графики и диаграммы в PDF отчётах
- [ ] Email-рассылка отчётов

### Приоритет 3 (Опционально)
- [ ] Расписание автоматических отчётов (cron jobs)
- [ ] История экспортов в БД
- [ ] Шаблоны отчётов (настраиваемые)
- [ ] Экспорт в Google Sheets

---

## 🐛 Troubleshooting

### Проблема: Файл не скачивается
**Решение**: Проверить `responseType: 'blob'` в axios запросе

### Проблема: Пустой Excel файл
**Решение**: Проверить фильтры - возможно, нет данных

### Проблема: 500 Internal Server Error
**Решение**: Проверить логи сервера (`console.error`)

### Проблема: Кириллица отображается некорректно
**Решение**: Для Excel - автоматически работает, для PDF - нужен кастомный шрифт

---

## 📚 Документация

Полная документация API: **EXPORT_API_GUIDE.md**

---

## ✨ Итоги

### Статус реализации: ✅ ГОТОВО

**Реализовано**:
- ✅ Экспорт аналитики ИКОП в Excel
- ✅ Экспорт студентов в Excel
- ✅ Экспорт отчётов в PDF (сводный и детальный)
- ✅ Поддержка фильтров
- ✅ Интеграция с frontend
- ✅ Обработка ошибок
- ✅ Документация API

**Работают endpoint'ы**:
1. `POST /api/admin/export/excel` ✅
2. `POST /api/admin/export/pdf/:type` ✅
3. `POST /api/admin/export/custom-polls` ✅ (уже был)
4. `POST /api/admin/polls/:id/export` ✅ (уже был)

**Frontend кнопки**:
- QualityDashboard: кнопка "Экспортировать" ✅
- PollResults: кнопки "Экспорт в Excel/PDF" ✅
- CustomPollsDashboard: кнопка "Экспорт списка" ✅

---

**Дата реализации**: 2026-01-15
**Разработчик**: AI Assistant
**Версия**: 1.0.0
