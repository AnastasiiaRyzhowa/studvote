# Руководство по тестированию системы аналитики опросов

## Что было реализовано

✅ **1. Обновлена модель Poll** (`server/src/models/Poll.js`)
- Добавлены поля для типов опросов: `topic`, `teacher`, `subject`, `organization`, `custom`
- Добавлена привязка к учебному процессу (subject_id, teacher_id, lesson_date)
- Реализован таргетинг по факультетам, программам, курсам, группам
- Добавлены метаданные пользователей в responses для срезов аналитики
- Реализован метод `addVote()` с сохранением метаданных
- Добавлен кэш аналитики для быстрого доступа

✅ **2. Создан сервис аналитики** (`server/src/services/analyticsService.js`)
- Функция `analyzePollResults()` - главная функция анализа
- Группировка результатов по факультетам, программам, курсам
- Вычисление средних оценок для каждой группы
- Генерация инсайтов (лидеры, тренды, явка)
- Поддержка разных типов опросов

✅ **3. Обновлен контроллер** (`server/src/controllers/pollController.js`)
- Добавлен метод `getPollAnalytics()` для получения аналитики
- Обновлен метод `vote()` для сохранения метаданных пользователей
- Добавлена проверка прав доступа к аналитике

✅ **4. Обновлены роуты** (`server/src/routes/pollRoutes.js`)
- Добавлен роут `GET /api/polls/:pollId/analytics`

✅ **5. Обновлена модель User** (`server/src/models/User.js`)
- Добавлены поля `faculty_name`, `program_name`, `group_name`

---

## Инструкция по тестированию

### Предварительные требования

1. MongoDB запущена
2. Сервер запущен: `cd server && npm run dev`
3. У вас есть JWT токен авторизованного пользователя

### Шаг 1: Запуск сервера

```bash
cd /Users/anastasiia/Desktop/studvote/server
npm run dev
```

Убедитесь, что сервер запустился без ошибок.

---

## Тестовые сценарии

### Сценарий 1: Создание опроса с новой моделью (тип: topic)

**Endpoint:** `POST /api/polls`

**Headers:**
```json
{
  "Authorization": "Bearer YOUR_JWT_TOKEN",
  "Content-Type": "application/json"
}
```

**Body:**
```json
{
  "title": "Оценка темы: Базы данных",
  "description": "Оцените, насколько понятно была объяснена тема",
  "type": "topic",
  "subject_id": "12345",
  "subject_name": "Основы программирования",
  "topic_name": "Реляционные базы данных",
  "teacher_name": "Иванов И.И.",
  "target_faculties": ["fit"],
  "target_programs": ["devops"],
  "target_courses": [2, 3],
  "questions": [
    {
      "id": "q1",
      "text": "Насколько понятно объяснена тема?",
      "type": "rating_1_5",
      "scale": ["Не понятно", "Плохо", "Средне", "Хорошо", "Отлично"],
      "required": true
    }
  ],
  "start_date": "2025-01-06T00:00:00Z",
  "end_date": "2025-01-13T23:59:59Z"
}
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "message": "Опрос успешно создан",
  "poll": {
    "_id": "...",
    "title": "Оценка темы: Базы данных",
    "type": "topic",
    ...
  }
}
```

---

### Сценарий 2: Голосование с метаданными

**Endpoint:** `POST /api/polls/votes`

**Headers:**
```json
{
  "Authorization": "Bearer YOUR_JWT_TOKEN",
  "Content-Type": "application/json"
}
```

**Body:**
```json
{
  "poll_id": "POLL_ID_FROM_STEP_1",
  "type": "form",
  "answers": {
    "q1": 5
  }
}
```

**Важно:** При голосовании система автоматически сохранит метаданные пользователя:
- `user_faculty`
- `user_faculty_name`
- `user_program`
- `user_program_name`
- `user_course`
- `user_group`
- `user_group_name`

**Ожидаемый результат:**
```json
{
  "success": true,
  "message": "Ответы сохранены",
  "points_earned": 10
}
```

---

### Сценарий 3: Получение аналитики по опросу

**Endpoint:** `GET /api/polls/:pollId/analytics`

**Headers:**
```json
{
  "Authorization": "Bearer YOUR_JWT_TOKEN"
}
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "analytics": {
    "overall": {
      "total_responses": 5,
      "average": 4.2,
      "distribution": {
        "1": 0,
        "2": 0,
        "3": 1,
        "4": 2,
        "5": 2
      }
    },
    "byFaculty": {
      "fit": {
        "name": "Факультет информационных технологий",
        "count": 3,
        "average": 4.3
      },
      "fmeo": {
        "name": "Факультет математики",
        "count": 2,
        "average": 4.0
      }
    },
    "byProgram": {
      "devops": {
        "name": "DevOps",
        "faculty": "Факультет информационных технологий",
        "count": 2,
        "average": 4.5
      },
      "data-science": {
        "name": "Data Science",
        "faculty": "Факультет информационных технологий",
        "count": 1,
        "average": 4.0
      }
    },
    "byCourse": {
      "2": {
        "count": 3,
        "average": 4.3
      },
      "3": {
        "count": 2,
        "average": 4.0
      }
    },
    "insights": [
      {
        "type": "leader",
        "icon": "🏆",
        "text": "Самая высокая оценка у Факультет информационных технологий: 4.3 ⭐"
      },
      {
        "type": "turnout",
        "icon": "⚠️",
        "text": "Низкая явка: только 20% студентов проголосовали"
      }
    ],
    "poll_type": "topic"
  }
}
```

---

### Сценарий 4: Создание опроса для оценки преподавателя

**Body для POST /api/polls:**
```json
{
  "title": "Оценка преподавателя: Иванов И.И.",
  "description": "Оцените работу преподавателя по нескольким критериям",
  "type": "teacher",
  "teacher_id": "67890",
  "teacher_name": "Иванов Иван Иванович",
  "subject_id": "12345",
  "subject_name": "Основы программирования",
  "target_groups": ["1234", "5678"],
  "questions": [
    {
      "id": "q1",
      "text": "Доступность объяснения материала",
      "type": "rating_1_5",
      "scale": ["Очень плохо", "Плохо", "Средне", "Хорошо", "Отлично"],
      "required": true
    },
    {
      "id": "q2",
      "text": "Организация занятий",
      "type": "rating_1_5",
      "scale": ["Очень плохо", "Плохо", "Средне", "Хорошо", "Отлично"],
      "required": true
    },
    {
      "id": "q3",
      "text": "Готовность ответить на вопросы",
      "type": "rating_1_5",
      "scale": ["Очень плохо", "Плохо", "Средне", "Хорошо", "Отлично"],
      "required": true
    },
    {
      "id": "q4",
      "text": "Рекомендуете ли вы этого преподавателя?",
      "type": "yes_no",
      "required": true
    }
  ],
  "start_date": "2025-01-06T00:00:00Z",
  "end_date": "2025-01-20T23:59:59Z"
}
```

---

### Сценарий 5: Организационный опрос

**Body для POST /api/polls:**
```json
{
  "title": "Выберите дату для мероприятия",
  "description": "Голосование за удобную дату проведения хакатона",
  "type": "organization",
  "lesson_date": "2025-01-15T00:00:00Z",
  "lesson_time": "14:00-18:00",
  "target_faculties": ["fit"],
  "questions": [
    {
      "id": "q1",
      "text": "Выберите удобную дату",
      "type": "multiple_choice",
      "options": ["15 января", "20 января", "25 января", "1 февраля"],
      "required": true
    }
  ],
  "start_date": "2025-01-06T00:00:00Z",
  "end_date": "2025-01-10T23:59:59Z"
}
```

---

## Проверка результатов в MongoDB

### Проверить структуру ответов с метаданными:

```javascript
db.polls.findOne({ _id: ObjectId("YOUR_POLL_ID") }, {
  responses: 1
})
```

**Ожидаемая структура:**
```json
{
  "responses": [
    {
      "user_id": ObjectId("..."),
      "answers": { "q1": 5 },
      "user_faculty": "fit",
      "user_faculty_name": "Факультет информационных технологий",
      "user_program": "devops",
      "user_program_name": "DevOps",
      "user_course": 2,
      "user_group": "1234",
      "user_group_name": "ИКБО-01-22",
      "submitted_at": ISODate("...")
    }
  ]
}
```

---

## Тестирование с помощью curl

### 1. Получить токен (авторизация):
```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "student@rudn.ru",
    "password": "your_password"
  }'
```

### 2. Создать опрос:
```bash
curl -X POST http://localhost:5000/api/polls \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "title": "Тест опроса",
    "type": "topic",
    "questions": [{
      "id": "q1",
      "text": "Оценка темы",
      "type": "rating_1_5",
      "required": true
    }],
    "end_date": "2025-01-20T00:00:00Z"
  }'
```

### 3. Проголосовать:
```bash
curl -X POST http://localhost:5000/api/polls/votes \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "poll_id": "POLL_ID",
    "type": "form",
    "answers": { "q1": 5 }
  }'
```

### 4. Получить аналитику:
```bash
curl -X GET http://localhost:5000/api/polls/POLL_ID/analytics \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## Возможные ошибки и решения

### Ошибка: "Опрос не найден"
- Проверьте правильность poll_id
- Убедитесь, что опрос действительно создан в базе

### Ошибка: "Нет доступа"
- Аналитику может видеть только создатель опроса или админ
- Проверьте, что вы авторизованы как создатель

### Ошибка: "Вы уже проголосовали"
- Используйте другого пользователя для тестирования
- Или создайте новый опрос

### Ошибка: Cannot read property 'faculty' of undefined
- Убедитесь, что у пользователя заполнены все обязательные поля:
  - faculty, program, course, group
  - faculty_name, program_name, group_name (могут быть null, но лучше заполнить)

---

## Следующие шаги

После успешного тестирования backend API:

1. ✅ Создать фронтенд компоненты для отображения аналитики
2. ✅ Добавить визуализацию срезов (графики, диаграммы)
3. ✅ Реализовать экспорт результатов (PDF, Excel)
4. ✅ Добавить фильтры и сортировку в аналитике

---

## Примеры тестовых данных

### Создать несколько тестовых пользователей с разными метаданными:

**Пользователь 1 (ФИТ, DevOps, 2 курс):**
```json
{
  "email": "student1@rudn.ru",
  "full_name": "Студент Один",
  "role": "student",
  "faculty": "fit",
  "faculty_name": "Факультет информационных технологий",
  "program": "devops",
  "program_name": "DevOps",
  "course": 2,
  "group": "ИКБО-01-23",
  "group_id": 1234,
  "group_name": "ИКБО-01-23"
}
```

**Пользователь 2 (ФИТ, Data Science, 3 курс):**
```json
{
  "email": "student2@rudn.ru",
  "full_name": "Студент Два",
  "role": "student",
  "faculty": "fit",
  "faculty_name": "Факультет информационных технологий",
  "program": "data-science",
  "program_name": "Data Science",
  "course": 3,
  "group": "ИКБО-02-22",
  "group_id": 5678,
  "group_name": "ИКБО-02-22"
}
```

**Пользователь 3 (ФМЭО, Экономика, 2 курс):**
```json
{
  "email": "student3@rudn.ru",
  "full_name": "Студент Три",
  "role": "student",
  "faculty": "fmeo",
  "faculty_name": "Факультет математики и экономики",
  "program": "economics",
  "program_name": "Экономика",
  "course": 2,
  "group": "ЭБ-01-23",
  "group_id": 9999,
  "group_name": "ЭБ-01-23"
}
```

Проголосуйте каждым пользователем, чтобы получить разнообразные срезы в аналитике!

---

## Статус реализации

✅ **ГОТОВО:**
- Модель Poll обновлена
- Сервис аналитики создан
- Контроллеры обновлены
- Роуты добавлены
- Модель User дополнена

🔄 **В ПРОЦЕССЕ:**
- Тестирование API

⏳ **СЛЕДУЮЩИЙ ЭТАП:**
- Фронтенд компоненты для аналитики

---

**Дата создания:** 6 января 2025  
**Версия:** 1.0  
**Автор:** Cursor AI




